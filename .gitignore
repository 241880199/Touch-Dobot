# OpenHaptics SDK (separate install)
OpenHaptics/

# Build artifacts
Touch_Client/x64/
Touch_Client/nul
*.exe
*.dll
*.ilk
*.pdb
*.obj
*.tlog/
*.lastbuildstate
*.recipe
*.suo
*.vcxproj.user
Browse.VC.db
Solution.VC.db
DocumentLayout.json
DocumentLayout.backup.json
fileList.bin

# IPCH
*.ipch

# MATLAB temp
Relay_Station/logs/

# Superpowers scratch
.superpowers/

# OS
Thumbs.db
Desktop.ini

# 标定【运行时状态】: 整个目录都不进库。活路径会被 app 重写 (CalibStore::dir() 从可执行文件
# 位置推目录, 与 CWD 无关), 且 calib_poses.txt 只追加、永不截断 ⇒ 提交它会让被文档引用的数字
# 随手漂、每次采集都把工作区弄脏。推导引用的数改引 tests/fixtures/ 下的【冻结快照】。
# ⚠ 磁盘上的活文件【保留】: force_calib.json 是 app 启动装载的标定, 删掉就得重标一次。
Touch_Client/calib/
# 仓库根的 calib/: exe 落在 Touch_Client\ 下一层时按同一套规则产生的运行时输出 (前导 / 锚定)
/calib/

# ===== 2026-09-24: 会话/证据日志（曾被 `git add -A` 误提交 138 个、约 60MB，已摘出）=====
# ⚠ 用【定向】模式，不用 *.log / *_out.txt 宽通配 —— 仓库里有 16 个被跟踪的
#   Docs/superpowers/evidence/.../_*_out.txt 是【入库证据】，宽通配会把它们藏起来。
Relay_Station/_matlab_session_*.log
Touch_Client/session_*.log
Touch_Client/_build_*.log
Touch_Client/_battest*.bat
Touch_Client/tests/_build_*.log
Touch_Client/tests/_fw_*.log
Touch_Client/tests/_harness_*.log
Touch_Client/tests/_t1_*.txt
Touch_Client/tests/_*.bat
Touch_Client/tests/_*.cpp
Touch_Client/tests/*.log
Hardware/stl.zip
