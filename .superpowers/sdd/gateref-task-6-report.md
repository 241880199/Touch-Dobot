# Task 6 报告 —— `A` 第三行守门：先查清 z 缺口

**任务:** `Docs/superpowers/plans/2026-09-21-gate-reference-and-moment-vote.md` Task 6
**性质:** **只读诊断。** 交付物是**判决与设计**，不是代码。**本次未改任何 `.cpp` / `.h`，
未跑客户端、未动机械臂、未新增采集。**
**落盘:** `Docs/superpowers/evidence/2026-09-21-z-gap-report.md`（全程取证）+
`Docs/superpowers/specs/2026-09-19-remaining-workflow.md` §3 / §3.1 / §4（开放项 C 的结论）

---

## 1. 判决

### **第 1 支 —— 「38~64 N」不是模型缺陷，也不是任何 z 差；开放项 C 可以关闭。**

一句话：**那个数是 `tol_M / |c_s_横向|` —— 力矩通道"能看见多大的 ΔFz"的探测下限。
它描述的是"一道假想的力矩门要多粗才看得见 z"，而力矩三个分量自 2026-09-21 起已经不投票
（`g_guardVote`）⇒ 那道门不存在。**

**⚠ 一句必须先说的话（对任务前提本身的更正）:** 计划 Task 6 写的是「开放项 C 记的是
"z 轴的缺口 ~38~64 N"」，Task 6 Step 2 又要求「列出能解释 38~64 N 的候选」。
**"能解释 38~64 N"这个问法预设了它是一个"差"，而仓库里没有任何文件把它记成差。**
它的三处落点（`.superpowers/sdd/runtime-guard-report.md` §3.1.1、
`ForceCompensation.cpp` 的 `g_guardVote` 注释块、`Config.h` 的 `FORCE_GUARD_TOL_FORCE_N`
注释块）**全部**是同一句算式：`ΔFz 要到 tol_M / |c_s_横向| = 38 ~ 64 N 量级，力矩通道才有机会
把它顶超限`。开放项 C 自己那一行的括注（"力矩通道也兜不住（`|c_s 横向|` 只有 0.5~0.8 mm）"）
也指向这个读法。**⇒ 候选 己（"38~64 N 本身是一个 z 差"）标为"不可判别"，因为它不是一次测量。**
本报告没有为这个不存在的"差"编造解释。

---

## 2. 证据链（逐条注明出处）

| 步 | 主张 | 出处 | 复现 |
|---|---|---|---|
| E1 | `38~64 N` = `tol_M / \|c_s_横向\|`，四份为 `38.4 / 60.1 / 53.0 / 63.7 N` | `.superpowers/sdd/runtime-guard-report.md` §3.1.1；`Touch_Client/force/ForceCompensation.cpp`（`g_guardVote` 段）；`Touch_Client/config/Config.h`（`FORCE_GUARD_TOL_FORCE_N` 段） | **✅ 逐位**：我由四份夹具（`Touch_Client/tests/fixtures/calib_poses_2026-09-19*.txt`）算出 `\|c_s_横向\| = 0.780572 / 0.499379 / 0.565795 / 0.471061 mm`、`tol_M/\|c_s_横向\| = 38.43 / 60.07 / 53.02 / 63.69 N`。同跑得到的 `σ(A)` 与 `Docs/superpowers/specs/2026-09-21-gate-reference-prereq.md` §7.2 的表**逐位相同** |
| E2 | 力矩三个分量已不投票 ⇒ 那个算法现在**没有门** | `ForceCompensation.cpp` 的 `g_guardVote = {true,true,false,false,false,false}`；同一段 2026-09-21 的补充原文"…力矩三个分量已经【不投票】…⇒ 几十牛那个洞【比原来更大了一点】"；`Config.h` 同段 | 读码 |
| E3 | `A` 的第三行**整行**只值 `4.028 N` | `force_calib.json`（`version 3`）的 `a_matrix` 第三行 `[−0.0055097, −0.0098905, −0.4104049]`，模 `0.410561 kg`；`m = \|det A\|^(1/3) = 0.415864 kg` ⇒ 整链重力 `4.080 N` | ✅ 算得。⇒ `38.4/4.028 = 9.5×`、`63.7/4.028 = 15.8×`。**要 38~64 N 的 ΔFz，第三行的模得是 `3.9~6.5 kg`，即实测的 9~16 倍** |
| E4 | 三轮 `@720` 数据上**没有** 38~64 N 量级的 z 差 | `Touch_Client/calib/calib_poses.txt` 三段带 `F720*` 的 `# attempt`（`21:58:46` / `22:12:28` / `22:18:42`，各 10 行 = 5 对原地复采） | **✅ 复现**（我的轮均值与前置复核 §2.1 的表逐位相同，例：`comp−@576` 的 `Fx` 2.260/1.806/2.074）。`\|comp_z−@720_z\|` 最大 **0.085 / 0.174 / 0.144 N**；`\|comp_z−@576_z\|` 最大 0.196 / 0.201 / 0.251 N |
| E5 | 与机械臂那套模型的 z 行之差 ≤ **0.160 N** | 同上数据；**我算**：拟合 `comp − @720 = c + K·g`，`K = A_臂 − A` | ✅ `K` 的 z 行模 `0.00777 / 0.00422 / 0.01636 kg` = `0.076 / 0.041 / 0.160 N`；x/y 行的模同量级小。噪声由**同姿态复采对**给（`σ_z = 0.0281/0.0242/0.0189 N`），逐分量 `se` 0.0010~0.0048 kg |
| E6 | 第三行**满、稳**（每次采集各一次拟合），且**不属于** `@576` 的"秩 2" | `Touch_Client/calib/calib_report.md` 的 `## 采集 <时间>` 各节（九节可用；`18:46:03` 那节的 `A` 全零，同节自写"decompose 没做成…这不是 0, 是【没有】"，不计） | ✅ 第三行模：`18:49:55` 0.415717 / `20:07:00` 0.413614 / `20:16:16` 0.412697 / `20:27:36` **0.433297（第三行模上唯一离群，该节各向同性比 1.50655 —— ⚠ 但它**不是**九节里最差的，见 §8.1）** / `20:52:00` 0.410949 / `21:02:15` 0.415812 / `21:58:46` 0.412013 / `22:12:28` 0.414491 / `22:18:42`（已安装）0.410561；四份夹具另算为 `0.414332/0.418826/0.411366/0.413265`。⇒ 十三次里除那一次全落在 `0.410561~0.418826 kg`。与另两行近正交（1.8°/0.9°） |
| E7 | 两个 `Fz` 是不同的量（但只值 ≈0.19 N） | 通道定义见 `Docs/机械臂资料/TCP_IP远程控制接口文档.md` 30004 布局表；数据见上 | ✅ `comp−@576` 的 z 恒正（`+0.129/+0.131/+0.153`），`comp−@720` 的 z 为 `+0.006/−0.082/−0.058` |
| E8 | 换帧只进力矩，不进力 | `Config.h` 容差注释块所引的 `\|Δc\| ≤ 14.15 mm`；力矩叉乘结构 | ✅ 把 `comp−@720` 的力矩差拟合成 `Δc × F` ⇒ `\|Δc\| = 70.0~72.1 / 74.4~78.2 / 171.2 mm`（**八种口径的范围**，界是 14.15 mm）、`R² = 0.220~0.230 / 0.224~0.234 / 0.583` ⇒ **不是换帧项**，是 `@720` 力矩自带的姿态无关偏置（前置复核 §2.3 已复现其漂移） |
| E9 | 原"秩 2"依据的对象 = **重力方向响应矩阵 `K` 的奇异值** | `Docs/superpowers/plans/2026-09-19-raw-channel-calibration.md` Task 10 Step 2；`.superpowers/sdd/runtime-guard-report.md` §3.3（已记该数只出现在计划书一处） | **✅ 逐位**：`12:38` 夹具给 `σ(K_576) = [0.212024, 0.201123, 0.008311]`，行模 `[0.211702, 0.201161, 0.013790]`，第三行比另两行小 **15.4 倍**。排除了两个同名的对象：`A` 的 `σ`（0.43/0.42/0.41）与 `P`（`@576=P·@1304+q`，我算 `0.497/0.471/0.020`）都对不上 |
| E10 | **`@720` 上该形状不复现** | 同上 | ✅ 见 §4 |

**未被采用的来源（按前置复核 §2.2③ / §6 与任务要求）:** `run-004 §4.5③` 的
`(0.144, 0.181, 0.055) N` **本报告未用**（只有 `0.181` 可复现，`0.055` 对不上）。

---

## 3. 候选逐条：signature 与数据的判决

| 候选 | 可判别 signature | 数据判决 |
|---|---|---|
| **甲. `A` 的第三行本身估错** | `comp − 参考量` 的重力响应矩阵 `K` 的第三行 = `A_参考 − A` 的 z 行，其模 ≈ 误差 (kg)；噪声由同姿态复采给 | **排除。** 实测 `0.0042 ~ 0.0164 kg`（`0.041 ~ 0.160 N`）；要 38~64 N 需要 `3.9 ~ 6.5 kg` ⇒ **差 240 ~ 400 倍**。另外第三行模在十三次拟合里除一次外都落在 `0.410561 ~ 0.418826 kg` |
| **乙. `@576` 的 `Fz` 与 `@720` 的 `Fz` 是两个不同的量** | 两者的 z 残差系统性不同（符号/幅度/对姿态响应都不同）；**并且两者之差必须与 z 的物理量级相容** | **前半支持、后半不支持。** 两个量确实不同（E7），但**差只有 ≈0.19 N**。⇒ **不是 38~64 N 的解释** |
| **丙. 换帧（原点/平移）在 z 方向的影响** | 只进力矩：`ΔM = Δc × F`，∝ 力、`F→0` 时消失；**力分量对它完全不敏感** | **按构造排除**（力与原点无关）；实测也排除（E8）：拟合 `\|Δc\|` 比界大约 4.9~12 倍、`R²` 低（`0.22~0.58`） |
| **丁. 参考量自己的 z 通道坏 ⇒ 比较无信息** | 参考量的 `K` 出现"第三行比另两行小一个量级"；且**差量**的 `K` z 行**继承同一种抑制** | **在 `@576` 上成立**（`K` 第三行小 7.7~15.4 倍；差量 z 行被抑制 14~39 倍）；**在 `@720` 上不成立**（`0.179× / 2.092× / 0.721×`；差量 z 行抑制比 `0.260 / 0.177 / 0.721`）。⇒ 见 §4 |
| **戊. z 上有一个没建模的力** | `@1304 − b_F − A·g` 的 z 随姿态有结构，且该结构在 `@576`/`@720` 上**同向同幅**（它是物理量，不是参考量特性） | **未见。** 能看到的 z 结构就是甲那张表（`0.0042~0.0164 kg`），与复采噪声同量级 |
| **己. 「38~64 N 本身是一个 z 差」** | **不可判别** —— 它不是候选：它的出处是一句**算式**（E1），不是一次测量。写不出"数据上该看到什么" | **不可判别，且已按 §1 更正前提** |

---

## 4. `@720` 上 z 响应的秩复测（要求的补充项）

**对象先钉死:** `@576` 的重力方向响应矩阵 `K`（`F = c + K·g`，每次采集拟合一次）的奇异值。
复现证据 = `12:38` 夹具给 `[0.212024, 0.201123, 0.008311]`（记录是 `[0.212 0.201 0.008]`），
第三行比另两行小 15.4 倍（四份为 7.7 / 10.3 / 11.3 / 15.4 倍）。

**复测结果（同一对象，`@720`）:**

| | 21:58:46 | 22:12:28 | 22:18:42 |
|---|---|---|---|
| `σ(K_@720)` (kg) | 0.030748 / 0.006117 / 0.000583 | 0.017165 / 0.006061 / 0.001666 | 0.027177 / 0.017531 / 0.002523 |
| `K` 三行模 (kg) | 0.0118 / 0.0286 / 0.0051 | 0.0075 / 0.0057 / 0.0157 | 0.0164 / 0.0227 / 0.0164 |
| **第三行 / max(另两行)** | **0.179** | **2.092**（第三行最大） | **0.721** |
| 对照 `K_@576` 同一指标 | 0.088 | 0.094 | 0.070 |

**⇒ 「第三行比另两行小 8~15 倍」这个形状在 `@720` 上不复现**（⚠ **定案范围 = 第 2、3 轮与
三轮合计**：`@720` 的 z 行模 `0.0051 / 0.0157 / 0.0164 kg`、逐分量 `se 0.0010 ~ 0.0027 kg`
⇒ 第 1 轮的 `0.179` 在约 2σ 以内，单独不定案。见 §8 更正）。
`@720` 呈现的是**另一个**事实：整个 `K` 小了一个量级（`@720` 三轮最大行模 `0.0286 kg` vs
`@576` 同批三轮最大行模 `0.2148 kg`）—— 它是**已经补偿过**的残差；但**小的是分母**：
**x/y 两行**从 `0.20 ~ 0.21 kg` 塌到 `0.0075 ~ 0.0286 kg`，**z 行几乎没动**
（`0.0148 ~ 0.0201 kg` → `0.0051 ~ 0.0164 kg`）⇒ **不是 z 单独坏，也不是 z 行被修好**。
（⚠ `0.0299` 是另一张矩阵的 y 行、`0.225` 是 `σ1`，见 §8 更正。）
差别最清楚的地方在**差量**上：`comp − @576` 的 `K` 第三行被抑制 **14~39 倍**，
`comp − @720` 的**没有被抑制**（`0.177~0.721`）。

**⇒ 对「`Fz` 不投票」这件事意味着什么（明写）:**

1. **原依据按原样不复现** ⇒ 代码里那两行"未复测 / 不许读成已验"**仍然是对的**，
   本次复测把"未复测"变成了"复测了，**按原样不复现**"。
2. **但这不足以为"把 `Fz` 加进投票"背书**: 那要消费一个**在真实外力下从未验证**的参考量
   （前置复核 §4.2① 已把它列为主要遗留风险），而且本议题手上只有**静止**数据。
3. **⚠ 本任务因此不改掩码**（判决是第 1 支 ⇒ 不实现任何守门；即使落在 2/3 支也不实现）。
   是否重开掩码是**另一个决策**，已写进 `2026-09-19-remaining-workflow.md` §3.1 的残余表。

---

## 5. 判不出来的部分（明写）

1. **`A` 的第三行的绝对精度** —— 仓库里**没有** z 方向的力真值。§4 量到的是**与机械臂那套模型的差**
   （≤ 0.160 N），而 `A_臂` 由本项目早先下发的负载参数决定（`…-raw-channel-calibration.md`
   Task 8 / Task 9 ④⑤）⇒ **两者同错则看不出来**。另一个界是本地失拟 `eps_甲 ≤ 0.287 N`（夹具口径，
   前置复核 §7.2）。
2. **"看不见"的门槛** —— z 噪声 σ 是 `0.0189 ~ 0.0281 N`（口径 = `rms(复采对差)/2`，见 §8.6）、
   5 个不同姿态 ⇒ 第三行斜率的 `se` 是 `0.0010 ~ 0.0027 kg`。**低于约 0.05 ~ 0.1 N 的第三行
   误差，这三轮分辨不出来**（改用常规两样口径时读成"约 `0.07 ~ 0.14 N`"）。
3. **`@720` 在接触/外力下的行为** —— 无任何仓库文件验证过（前置复核 §4.2①）。
4. **`Fz` 不投票该不该改** —— 见 §4 第 3 条。
5. **一个建模假设（自审时发现，明写）** —— §4 的 `K = A_臂 − A` 这一步隐含
   "`@720` 与 `@1304` 在同一传感器坐标系、同一原点"（否则差里会多一个旋转/平移项）。
   仓库里**没有文件**直接确认这一点；间接旁证是 x/y 行的模也只有 `0.007~0.030 kg`
   （若坐标系不同，这三行不会都这么小）。**这一条是假设，不是文件。**

**⇒ 下一个能判别的测量（不需要动负载）:** 在同一批静态姿态上同时采 `@1304` / `@720` / `@576`，
姿态覆盖要包含"重力尽量落在传感器 z 轴"与"尽量落在 x/y 轴"两端，做**同一个** `K` 拟合，
判据是 `K`（`comp − @720`）的**第三行模**：仍在 `0.02 kg`（0.2 N）量级 ⇒ 维持"接受"；
显著变大（如 ≥ `0.1 kg` = 1 N）⇒ 第三行确实错了，那时再谈判据。
不需要动负载的理由：这一路比的是**两套模型的重力响应之差**，两者都靠**原地改姿态**激发，
负载参数一个字节都不用改。

---

## 6. 改动的文件

| 文件 | 改动 |
|---|---|
| `Docs/superpowers/evidence/2026-09-21-z-gap-report.md` | **新建**（全程取证：出处、复现方法、候选 signature、秩复测） |
| `Docs/superpowers/specs/2026-09-19-remaining-workflow.md` | §3 开放项 C 行改为已关闭；**新增 §3.1**（结论 + 三条残余）；§4 决策点" C 评估之后"改为已定 |
| `.superpowers/sdd/gateref-task-6-report.md` | 本报告 |
| **未改** | 任何 `.cpp` / `.h`；任何夹具；任何采集 |

两份文档都不含仓库内代码行号与计数；稳定文档按**节名**引用。

---

## 7. 自审发现与顾虑

1. **前提本身需要更正（最重要的一条）** —— 计划 Task 6 与任务简报把 `38~64 N` 当成一个 z 差。
   我**没有**按这个读法去找解释，因为**没有文件支持它是差**；报告里明写了这一点，
   并把"己"标为**不可判别**而不是"疑似"。若评审认为我应当直接给出第 1 支之外的判决，
   请先看证据报告 §2.1（那张表能逐位复现）。
2. **`eps_甲` 我没有重算** —— 用的是前置复核 §7.2 的 `0.287098 N`（那份已被独立复现过）。
   本报告对它的依赖只有一处（§5 第 1 条），且那句是"另一个界"。
3. **`K` 的拟合只有 5 个不同姿态（10 行 = 5 对）** —— 4 个参数压在 5 个点上，估计是确定的
   但不宽裕。我用**同姿态复采对**做噪声、给逐分量 `se`，没有用拟合残差当噪声
   （残差里含模型失拟，会把 `se` 抬高）。这一口径是保守的（`se` 偏小 ⇒ 更容易说"显著"），
   而我的结论是"不显著"，所以口径方向对我**不利**，不是有利。
4. **`2026-09-20 20:27:36` 那次拟合的第三行离群**（`0.433297 kg`）—— 我没有把它从
   "第三行稳定"这句话里拿掉，而是在证据报告 §4.1 明确标出它是十三次里唯一离群的一次
   （该节的各向同性比 1.507，也最差）。**若评审要求更严的口径，其余十二次的范围是
   `0.410561 ~ 0.418826 kg`。**
5. **★ 我第一版把 `calib_report.md` 各节的采集时间按位置猜错了**（该文件的节标题本身带时间，
   而我按"第几节"去对），自审时发现并按节标题改正。⇒ **这也是本任务自己踩到的一个"凭记忆/
   按位置办事"的坑**，与本项目记账的那条教训同源（见 `verify-premises-at-the-source`）。
   两份文档现在一律带节标题的采集时间，并在证据报告 §4.1 留了一行引用纪律。
6. **我没有动掩码，也没有实现任何守门。** 判决落在第 1 支，且即使落在 2/3 支也不实现 ——
   这是任务明写的边界。
7. **顾虑（不是本任务的判决）:** §4 第 1 条让代码里那句"未复测"有了实测答案，
   而答案是"按原样不复现"。**这意味着 `Fz` 不投票这件事现在站在一个已被复测推翻的理由上。**
   我在两份文档里都把它写成**残余**而不是把它转成一个新判决 —— 因为改掩码要消费一个
   "接触下未验证"的参考量。**请评审确认这个处理是否合适**；若要重开掩码，那应当另开一项。

---

## 8. 评审修正（2026-09-21，逐条）

**性质: 只改文档，不改判决、不改任何被复核过的数。** 本节的每一项都在
`Docs/superpowers/evidence/2026-09-21-z-gap-report.md` 里落到了对应的段，
§3.1 的那一条另落到了 `Docs/superpowers/specs/2026-09-19-remaining-workflow.md`。
**未改任何 `.cpp` / `.h`。** 下面凡标"我复核"的，都是我这次亲手重算的（脚本见本节末）。

### 8.1（Important）"九节里最差的一次"不成立

- **原话**（证据报告 §4.1 表格 `2026-09-20 20:27:36` 那一行）:
  "`0.433297` ← **唯一一个离群的**（该节 `σ` 为 `0.527000/0.428288/0.349807`，各向同性比 1.507，
  **是九节里最差的一次**）"
- **我复核:** 九节各向同性比逐个复算 = `1.07275 / 1.68953 / 1.18032 / 1.50655 / 1.06360 /
  1.06450 / 1.07708 / 1.08283 / 1.04981` ⇒ **最大的是 `20:07:00` 的 `1.68953`**，
  `20:27:36` 的 `1.50655` 只排第二。`20:07:00` 的行模 `0.526084 / 0.623547 / 0.413614`
  （x/y 被抬起来，第三行正常）—— 与评审核对**逐位相同**。
- **改法:** 删掉"最差的一次"，改为"**第三行的模上**唯一一个离群的"；并在表后加一段更正，
  写明**差的是两次**（`20:07:00` 与 `20:27:36`），以及**在这两次里稳的只有第三行**，
  x/y 两行并不稳 ⇒ 引用"第三行稳定"时不能读成"这几次拟合整体都合格"。
- **判决不变**（第三行的模确实稳定，与 `m` 差 <1.3%）。

### 8.2（Important）`@720` 形状比要带 `se`，且分子/分母要分开说

- **原话（改前，证据报告 §5.2）**:
  > **⇒ 判决：「第三行比另两行小 8~15 倍」这个形状在 `@720` 上不出现**（`0.179 / 2.092 / 0.721`；
  > 其中一轮第三行**最大**）。它成立的是**另一个**事实：`@720` 的**整个** `K` 小了一个量级
  > （最大行模 `0.0299 kg` vs `@576` 的 `0.225 kg`）—— 因为 `@720` 是**已经补偿过**的残差，
  > 三个方向都小，**不是 z 单独坏**。
- **原话（改后，同处）**:
  > **⇒ 判决：「第三行比另两行小 8~15 倍」这个形状在 `@720` 上不出现**（`0.179 / 2.092 / 0.721`；
  > 其中一轮第三行**最大**）。
  >
  > **⚠ 但要带不确定度读，而且它为什么"不出现"要说清是分子还是分母（本轮评审补）:**
  >
  > 1. **这三个比值没有不确定度就说不成一句话。** `@720` 的 **z 行模是 `0.0051 / 0.0157 /
  >    0.0164 kg`**，**逐分量 `se` 是 `0.0010 ~ 0.0027 kg`**（口径见 §4.2 与 §附 ④）。
  >    ⇒ **第 1 轮的 `0.179` 只比旧参考量的带（`@576` 三轮的 `0.070 ~ 0.094`）高出约 2 倍、
  >    在约 2σ 以内 —— 它单独不定案。** 定案的是**第 2、3 轮**（`2.092` 与 `0.721`）**与三轮合计**。
  >    所以"这个形状在 `@720` 上不复现"这句话，证据来自第 2、3 轮 + 合计，**不是**三轮平权。
  > 2. **"整个 `K` 小了一个量级"要说清减的是哪一部分：减的是分母，不是 z 行被修好。**
  >    **z 行几乎没动**：旧参考量三轮 `0.0148 ~ 0.0201 kg` → `@720` 的 `0.0051 ~ 0.0164 kg`；
  >    **塌下去的是 x/y**：旧 `0.20 ~ 0.21 kg`（每轮取大的那一行）→ `@720` 的
  >    `0.0075 ~ 0.0286 kg`。⇒ 所谓"z 不再被结构性压小"**说的是分母（x/y）缩了**，
  >    **不是** z 行被修好了。
  >    （⚠ 口径更正: `@720` 三轮的**最大行模是 `0.0286 kg`**……）
- **判决/结论没有被改弱成另一件事**：仍是"**按原样不复现**"，只把**强度**（第 1 轮不单独作数）
  与**机制**（减的是分母）改成与数据相符；同一句也补进了 §5.2 第 3 条、§8 的"不支持的读法"
  与 `2026-09-19-remaining-workflow.md` §3.1 的残余表。
- **我复核:** `@720` 三轮 z 行模 `0.00511 / 0.01568 / 0.01635`；
  旧参考量三轮 z 行模 `0.01867 / 0.02013 / 0.01478`（= 评审的 `0.0148 ~ 0.0201`）；
  x/y 每轮取大者：旧 `0.21153 / 0.21484 / 0.21132`、`@720` `0.02859 / 0.00750 / 0.02269`。
  评审给的每一个数都对得上。

### 8.3（Minor）换帧拟合的第 1 轮数复现不出来

- 原写第 1 轮 `69.6 mm / R² 0.210 / rms 0.253`、第 2 轮 `76.7 / 0.223 / 0.268`。
- **我复核**（八个变体：`A`、`c_s`、`b_M` 各取"已安装"或"本轮现拟合"，2×2×2）:
  第 1 轮 `70.0 ~ 72.1 mm`、`R² 0.220 ~ 0.230`、rms `0.249 ~ 0.252`；
  第 2 轮 `74.4 ~ 78.2 mm`（其中一支恰为 `76.8`）、`R² 0.224 ~ 0.234`、rms `0.264 ~ 0.267`；
  第 3 轮八支同值 `171.2 / 0.583 / 0.276`。**评审的范围逐位对上；`69.6` 在任何一支下都不出现。**
- **改法:** 表改成**八种口径的范围**，并在 §附 ⑦ 列出八个变体（可重跑）；
  正文里的"高出 5 ~ 12 倍"改成"高出约 4.9 ~ 12 倍"（`70.0/14.15 = 4.95`，**不是** 5 倍以上）。
- **判决不变**：八种口径下第 1 轮都 ≥ 界的 4.9 倍、`R²` 都 ≤ 0.23 ⇒ 换帧（丙）仍排除。

### 8.4（Minor）`P` 的奇异值

- 原写 `0.499 / 0.474 / 0.016`，**我复核**（`12:38` 夹具，`@576 = P·@1304 + q` 最小二乘）
  得 `0.496995 / 0.471344 / 0.020388` ⇒ 已改为 **`0.497 / 0.471 / 0.020`**，并注明了夹具。
  识别不受影响（两者都离 `0.21` 量级很远）。

### 8.5（Minor）出处的三处不严

- **(a) "最大行模 `0.0299 kg`" / 对照 `0.225`** —— `0.0299` 是 §4.2 那张**另一个矩阵**
  （`comp − @720`）的 `21:58:46` y 行；`@720` 三轮的真实最大行模是 **`0.0286 kg`**。
  对照侧的 `0.225` 是 `15:25` 的 **`σ1`（奇异值）**，而 `@576` 同批三轮最大**行模**是
  `0.2148 kg`（四份夹具里最大 `0.2250 kg`）。已按此改写，并写明"奇异值与行模不是同一个量"。
  **我复核**：`0.02986` 确实出现在 `comp − @720` 的 `21:58:46` y 行上。
- **(b) "30 个姿态"** —— 是 **30 行 = 3 轮 × 每轮 5 个不同姿态**（三轮的姿态集**互不相同**，
  我核过 `calib_poses.txt` 三段）。已改，且与本文别处的"5 个不同姿态"一致。
- **(c) "两个 z 不同号"** —— 三轮里**只有 2 轮**不同号（第 1 轮 `comp−@576` 的 `+0.129` 与
  `comp−@720` 的 `+0.006` 同为正）。§3 的候选表与 §4.3 都改成"**倾向于**不同号（3 轮里 2 轮）"，
  括注里的数原稿已经是对的，保留。

### 8.6（Minor）σ / `se` 的口径

- 原稿没有写 σ 的定义。**我复核**: `0.0281 / 0.0242 / 0.0189 N` 正是 **`rms(复采对之差)/2`**
  （对零点折半）；常规两样口径（对均值、`ddof=1`、除 `√2`）在同一批差上是
  **`0.0438 / 0.0152 / 0.0299 N`** —— 与评审给的三个数**逐位相同**。
- 也确认了"逐分量 `se`"这一列**把同一个 z 的 σ 摊到三个分量上**（`se_i = σ·√[(XᵀX)⁻¹]_{ii}`），
  三个分量的 σ 没有分开估。
- **改法:** 在 §4.2 加一段"口径必须写明"，在 §附 ④ 注明；并在 §7 第 2 行注明
  **改用常规口径时那句"约 `0.05 ~ 0.1 N`"要读成"约 `0.07 ~ 0.14 N`"**（评审的 1.4×，即 `√2`）。
- 一处**不与评审相同**的措辞保留：评审写"about 1/√2 below"，这作为**构造上的期望关系**成立
  （`rms/2` 与 `样本标准差/√2` 相差 `√2`），但**实现值可以两边偏** —— 第 2 轮 `0.0242`
  就**大于**常规值 `0.0152`。所以"方向保守"是**口径级**的陈述，不是每一轮都成立。文中按此写。

### 8.7（Minor）`t` 值

- 原写"三轮里两轮的 `t` 落在 2~5，一轮落在 0.7~2"。
- **我复核**: 按 z 分量 `t = |k_z| / se_z`（`se` 用本文口径）是 **`2.766 / 2.056 / 4.080`**
  ⇒ 评审的 `2.77 / 2.06 / 4.08` 正确，**三轮都在 2~5 这一档**，原稿那句话在两种口径下都对不上。
- 评审另给的"逐分量 `se` ⇒ `3.33 / 2.53 / 13.39`": **我只复现到近似** ——
  用**逐分量**的 σ（`rms/2`）并对该行三个分量取**最大** `t`，我得到 **`3.34 / 2.54 / 13.43`**
  （`3.33` 差 0.01 内，`13.39` 差 0.04）。结论与评审一致，但**这三个数我没有逐位复现**，
  所以文中写成"同一张表里**最大的** `t` 是 `3.33 / 2.53 / 13.39`（评审口径）"，
  并同时给出我逐位复现的那一组。
- **改法:** §4.5 改成"两种口径都给数"，并明写**判决不靠 `t`**，靠 z 结构的**模**
  （`0.0042 ~ 0.0164 kg`）比所需的 `3.9 ~ 6.5 kg` 小两个量级。

### 8.8（Minor）§0 的标题句与它引的注释矛盾

- **原话（§0 判决段）**: "⇒ 这个数现在**连"一道假想的门"的下限都不是**（同一段注释里写明了这一点）"
- 它引的注释（`Config.h` 的 `FZ`/容差块与 `ForceCompensation.cpp` 的 `g_guardVote` 段）写的是：
  "**数字一个都没变**（它是"力矩门**若有**，要多大的 z 差才看得见"），变的只是：**现在没有
  这道门**。"
  ⇒ 原句把"门不存在"错写成了"这个数连下限都不是"。**改法:** 用本文 §2.1 已有的措辞，
  写成"那个算法**现在是一道不存在的门**"，并注明**数字一个都没变**。

### 8.9（Minor）一处单侧措辞

- **原话**（§5.2 第 1 条）: "所以"**那一侧的 z 通道坏了**"是个**有内容**的判断"
- 仓库明写这一条**没有分开、不许猜**（`…-raw-channel-calibration.md` Task 9 那段
  "若日后的闭环要用 z 行，先单独解释它为什么小"；`g_guardVote` 注释块照抄了"没有分开"）。
  **改法:** 中性化 —— 改成"它说的是"**`@576` 的 z 响应与刚性体不符**"这件**可检验的事**"，
  并紧接着写明**它不区分成因**（`@576` 报得坏 / 机械臂 z 补偿太强），**不能读成"那一侧的量坏了"**。
  这条**不进判决**，改前改后判决都是第 1 支。

### 8.10 复核用的脚本与口径

复核脚本在系统临时目录（`…/Temp/zgap/`，**不入库**）：`lib.py`（解析与最小二乘）、
`t10.py`（`K_576` 的 σ 与 `P` 的 σ）、`t13.py`（三轮的 `K`、两种噪声口径）、
`t14.py`（换帧拟合的八个变体）、`t20.py`（`se` 与 `t` 的两种口径）、`t21.py`（九节的
各向同性比与行模）。**没有跑构建、没有跑测试套件、没有动机械臂、没有新增采集。**

---

## 9. 第三轮评审修正（2026-09-21，逐条）

**性质: 只改文档措辞与出处，不改判决、不改任何被复核过的数。** 两项交付物都改了：
`Docs/superpowers/evidence/2026-09-21-z-gap-report.md`（§0 第 2 行、§4.1 更正段、§4.5 的 `t` 口径、
§5.2 第 1/2 条、§5.2 第 3 条、§6 第 4 条、§8 一条、§附 ④）与
`Docs/superpowers/specs/2026-09-19-remaining-workflow.md`（§3.1 残余表里"`Fz` 不投票的理由是否还成立"那一行）。
**未改任何 `.cpp` / `.h`；未跑构建、未跑测试套件、未动机械臂、未新增采集；没有落盘任何标定文件。**
本节凡"我复核"的都是这次亲手重算的（脚本在系统临时目录 `…/Temp/zgap2/`，**不入库**：
`an6.py`（`calib_report.md` 九节的 `A` 与四份夹具的 `A`）、`an2.py`/`an3.py`/`an4.py`/`an7.py`
（三轮的 `K`、两种 `se` 与 `t` 口径、`@576`/`@720` 行模）、`an.py`（姿态去重））。

### 9.1（Important）离群那一次被抬起来的行 —— 上一轮自己改错的地方

- **改前（§4.1 更正段的错处）**:
  > **差的拟合有两次**，不是一次。这两次里**被抬起来的是 x/y 两行**：
  > `20:07:00` 的行模是 `0.526084 / 0.623547 / 0.413614`（x/y 抬到 0.53/0.62，第三行仍正常）；
  > `20:27:36` 的是 `0.417499 / 0.470609 / 0.433297`。
  > ⇒ "第三行的模稳定"这句话只用第三行的模在撑：在这两次拟合里，稳的**只有第三行**……
- **我复核（`calib_report.md` 九节逐节解析 `A`，四份夹具 `Touch_Client/tests/fixtures/calib_poses_2026-09-19*.txt`
  自拟合 `F = b_F + A·g`）**：13 次拟合的行模——
  - `20:27:36`（`A` 第 1/2/3 行 = `[+0.3410678,+0.2407769,+0.0021734]` / `[−0.2341257,+0.4082107,+0.0047189]` /
    `[−0.1161338,+0.1114268,−0.4022973]`）⇒ `0.417499 / 0.470609 / 0.433297`；
  - 其余 **十二次**的第三行模带 = `0.410561 ~ 0.418826 kg`（含四份夹具的 `0.414332/0.418826/0.411366/0.413265`）
    ⇒ `0.433297` **高于其中每一个**；
  - 其余十二次的 **x** 行模范围 = `0.405861 ~ 0.526084 kg`、**y** 行模范围 = `0.412721 ~ 0.623547 kg`
    ⇒ `20:27:36` 的 `0.417499 / 0.470609` **都落在带内**。
  ⇒ 派工单说的"`20:27:36` 被抬起来的是第三行自己、x/y 在带内"**逐位成立**；改前的"这两次里被抬起来的是
  x/y 两行"对 `20:27:36` **不成立**，而且与它上面那张表自己标的"第三行的模上唯一一个离群的"相矛盾。
- **改后**：把"被抬起来的行"按两次分开写（`20:07:00` = x/y；`20:27:36` = 第三行自己），并写明
  "两次都不能读成'这次拟合整体都合格'"、"『第三行的模稳定』这句在 `20:27:36` 那次恰恰不成立"。
  §6 第 4 条（同一主张的呼应处）同步改：原写"**两次拟合差，稳的只有第三行**"，改为按行分开点名的写法。
- **⚠ 派工单里我复现不出来的半句:** 派工单说 `20:16:16` 的"isotropy 比那两个点名的更接近紧凑簇"
  （"much closer to the two named ones than to the tight cluster"）。**这一句我复现不出来**：
  13 次拟合的各向同性比排序是 `1.04981 / 1.06360 / 1.06450 / 1.06546 / 1.07275 / 1.07708 / 1.08148 /
  1.08283 / 1.12004 / 1.14071 / **1.18032** / 1.50655 / 1.68953` ⇒ `20:16:16` 的 `1.18032` 与
  点名的两次差 `0.326 / 0.509`，而**与紧凑簇的上端只差 `0.097`（与次高的一次只差 `0.040`）** ——
  按各向同性比它是**更靠近紧凑簇**那一侧。所以我**没有采用**那句话作为理由，只采用了它的**要求**
  （"要么把它写进来，要么写明判据"）：文中写明判据是**行模出带**，并写明 `20:16:16`
  的 x/y 行**也出带**（`0.436071 / 0.479725`；其余十次的 x 带 `0.405861 ~ 0.433214`、
  y 带 `0.412721 ~ 0.438778`），它与那两次的区别是**第三行没有出带**（`0.412697` 在带内）
  ⇒ 三次都列出来，"两次"不会被读成穷举。

### 9.2（Minor）行/姿态计数

- **改前**: `（**30 行 = 3 轮 × 每轮 5 个不同姿态**，三轮的姿态集互不相同）`
- **我复核（`Touch_Client/calib/calib_poses.txt` 三段带 `F720*` 的 `# attempt`，逐行读六坐标）**:
  每段 10 行、每段 5 个不同姿态（`repeat: first=1,3,5,7,9 seconds=2,4,6,8,10` ⇒ 每姿态 2 次原地复采）；
  30 行合计**去重后 14 个姿态** —— 第 1 轮与第 2 轮**共用 1 个姿态**
  `(-52.046, 69.014, -159.228, -170.006, -42.257, 529.998)`（六坐标逐位相同），
  三段姿态集的**两两交集**是 `1 / 0 / 0` ⇒ "互不相同"不成立；`3 × 5 = 15 ≠ 30` 也不平。
- **改后**: `（**30 行 = 3 轮 × 每轮 10 行**；每轮 10 行 = **5 个不同姿态 × 2 次原地复采**，三轮合计
  **14 个不同姿态** —— 第 1 轮与第 2 轮**共用 1 个姿态**，所以三轮的姿态集**不互不相交**）`

### 9.3（Minor）借来的 `se` 指到一张没有它的表

- **改前（§5.2 第 1 条）**: `**逐分量 `se` 是 `0.0010 ~ 0.0027 kg`**（口径见 §4.2 与 §附 ④）。`
- **我复核（§4.2 那张表的 `se` 列，逐值）**: 三轮九个值是
  `0.00234 / 0.00478 / 0.00199`（`21:58:46`）、`0.00255 / 0.00221 / 0.00133`（`22:12:28`）、
  `0.00270 / 0.00190 / 0.00100`（`22:18:42`）⇒ **整列跨 `0.0010 ~ 0.00478 kg`**（派工单给的 `0.00478` 对），
  其中**配 `g_z` 的系数**三轮是 `0.00199 / 0.00133 / 0.00100` ⇒ **`0.0010 ~ 0.0020 kg`**（派工单给的也对）。
  而且那张表是**差量 `comp − @720`** 的矩阵，本节的对象是 `@720` **自己**的矩阵。
- **⚠ 派工单里我复现不出来的半句:** "The `0.0027` upper bound appears nowhere in it" —— **`0.00270` 就在那张表里**，
  是 `22:18:42` 那一行的 **x 分量** `se`（同一行是 `0.00270 / 0.00190 / 0.00100`）。
  所以准确的写法不是"那张表里没有 `0.0027`"，而是"`0.0027` 不是那张表的 **z 分量** `se`"。
- **改后**: 明写这是**借用邻表**（并写明是哪张表、与本节的对象不是同一个矩阵），给出该表 z 行的
  `0.0010 ~ 0.0020` 与整列的 `0.0010 ~ 0.00478`，并写明原稿的上界 `0.0027` 是那张表第 3 轮 **x 分量**的值。
  随之把"在约 2σ 以内"改成可复算的写法：`0.0051 / 0.0020 = 2.6σ` ⇒ 约 `2 ~ 3σ`（§0 第 7 行、§5.2 第 3 条、
  §8 的三处同口径呼应同步改）；`2026-09-19-remaining-workflow.md` 那一行改写成"`se` 是**借用邻表的量级**
  （哪张表、哪个分量、取到哪一列，写明在证据报告 §5.2）"，不再复述那三个数，避免两处再分叉。

### 9.4（Minor）`t` 值没有口径

- **改前（§4.5）**: `同一张表里**最大的** `t` 是 **3.33 / 2.53 / 13.39**。`（无出处、无口径）
- **我复核并复原了这组数的算法**: 上一轮报告 §8.7 说它"只复现到近似（`3.34 / 2.54 / 13.43`）"，
  并把口径写成"用逐分量的 σ 并对该行三个分量取最大" —— **按那个写法我复算出来的是
  `2.768 / 2.056 / 4.857`，不是评审的那一组**。真正能**逐位**复原评审那一组的口径是：
  σ **按分量一一对应**（`g_x` 的系数配 x 分量的 σ、`g_y` 配 y、`g_z` 配 z，`se_j = σ_j · √[(XᵀX)⁻¹]_jj`），
  再对该行三个分量取最大 ⇒ **`3.3293 / 2.5253 / 13.3875`**（`21:58:46` 的最大项在 x、`22:12:28` 在 y、
  `22:18:42` 在 y）。⇒ 派工单给的那一组**是对的**，而上一轮报告的 `3.34 / 2.54 / 13.43`
  与它给的口径**都不对**（这一步是上一轮自己引入的错）。
- **改后**: 文中把这一组标成**评审口径**并把口径写全（含配对规则与复算值 `3.3293 / 2.5253 / 13.3875`），
  `§附 ④` 补上同一条口径（原来只写"若逐分量估 σ，`t` 会变"）。本文自己逐位复现的那一组
  （`2.77 / 2.06 / 4.08`）保持原样不动。

### 9.5（Minor）"z 行几乎没动"这个 gloss 过宽

- **改前（§5.2 第 2 条）**: `**z 行几乎没动**：旧参考量三轮 `0.0148 ~ 0.0201 kg` → `@720` 的 `0.0051 ~ 0.0164 kg`；`
- **我复核**: `@576` 的三轮 z 行模 = `0.018666 / 0.020132 / 0.014779`，`@720` 的 = `0.005107 / 0.015682 / 0.016354`
  ⇒ 第 2、3 轮基本没动，**第 1 轮 `0.018666 → 0.005107`，缩了 `3.66` 倍** —— 而第 1 轮正是同一段里
  写着"不单独作数"的那一轮。原稿的两个范围都正确，错的是"几乎没动"这个概括。
- **改后**: 保留范围与**分子/分母**那个区分（上一轮加的，未删），把 gloss 改成"**z 行减得少得多，
  但不是"没动"**"，并逐轮点出第 1 轮的 `0.0187 → 0.0051`（约 `3.7` 倍）与"不能当三轮平权读"。
  `2026-09-19-remaining-workflow.md` 同一句同步改。

### 9.6 我复核后**没有**动的数（本轮硬约束）

判决（第 1 支）与下列数**一个都没动**，并且我这次重算全部与文件逐位相同：
`tol_M / |c_s_横向|` 的四个值与 `38.4 / 60.1 / 53.0 / 63.7 N`；`4.028 N`（`0.410561 × 9.81`）；
`A_臂 − A` 的 z 行 `0.0078 / 0.0042 / 0.0164 kg`（`0.076 / 0.041 / 0.160 N`）与它的逐分量 `se`；
`K_@576` 与 `K_@720` 的两组奇异值；13 次拟合的第三行模与十二次带 `0.410561 ~ 0.418826 kg`；
§4.2 的 `σ = 0.0281 / 0.0242 / 0.0189 N` 与两种 `σ` 口径；§4.4 的八个变体范围。

### 9.7 本轮**没动**、但同源的一处（留给下一轮判）

证据报告 §7 第 2 行仍写着"第三行斜率的 `se` 是 `0.0010 ~ 0.0027 kg`" —— 与 §5.2 改前的
是**同一个借用区间、同一个上界**。派工单的 Fix 3 只点了 §5.2，所以**我没有动它**：
改它要连带重算那句"低于约 `0.05 ~ 0.1 N` 的第三行误差……分辨不出来"（`0.05/9.81 ≈ 0.0051 kg`、
`0.1/9.81 ≈ 0.0102 kg`，与 `se` 的对应关系原本就不清楚）以及紧随其后的 `√2` 注，
已超出"措辞与出处"这一轮的范围。**明写在这里，供下一轮决定。**
