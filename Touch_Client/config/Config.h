#pragma once

// ===================================================================
//  配置共享说明
//  - 单一数据源: system_config.json (同目录)
//  - C++ 侧编译时常量: 需与 JSON robot/relay/safety_bounds/connection 段保持同步
//  - MATLAB 侧: relay_config.m 运行时从 JSON 读取 (无需手动同步)
//  - 修改共享参数 (IP/端口/安全边界) 时请先编辑 JSON, 再更新此文件
// ===================================================================

namespace Config {
    // ========== 窗口参数 ==========
    const int WINDOW_W = 1024;
    const int WINDOW_H = 768;

    // ========== Touch 设备坐标范围 ==========
    const double MAX_ABS = 150;
    const double DEV_X_MIN = -MAX_ABS, DEV_X_MAX = MAX_ABS;
    const double DEV_Y_MIN = -MAX_ABS, DEV_Y_MAX = MAX_ABS;
    const double DEV_Z_MIN = -MAX_ABS, DEV_Z_MAX = MAX_ABS;
    const double CENTER_X = (DEV_X_MIN + DEV_X_MAX) / 2.0;
    const double CENTER_Y = (DEV_Y_MIN + DEV_Y_MAX) / 2.0;
    const double CENTER_Z = (DEV_Z_MIN + DEV_Z_MAX) / 2.0;

    // ========== 安全边界（机械臂用户坐标系，单位mm） ==========
    // 初始值设为保守范围，根据实际环境调整
    // CR3 工作半径 620mm，覆盖全工作空间并保留边界余量
    // 用户坐标系安全边界 (机器人静止位姿 ~(-103,-153,381))
    const double SAFE_X_MIN = -300.0, SAFE_X_MAX = 250.0;
    const double SAFE_Y_MIN = -350.0, SAFE_Y_MAX = 250.0;
    const double SAFE_Z_MIN = 140.0,  SAFE_Z_MAX = 500.0;
    const double SAFE_BOUNDARY_BUFFER_RATIO = 0.2; // 20%边界缓冲区，线速度衰减

    // ===== 工作空间边界【钳位 + 拒绝】的总开关 (2026-09-21, 用户现场决定关闭) =====
    //   true  = 开: 越界的目标被钳到边界上 (X/Y/Z 那个盒子), 且越界的命令被【拒绝】;
    //   false = 关【当前】: 【本开关管的那两道】(下 ①②) 不再以任何方式拦 —— 目标原样下发。
    //     ⚠ 但【不是"边界整个没了"】: 姿态钳位 (下 ③) 独立于本开关, 关掉它照样在夹 (见下)。
    //
    // 【为什么是一个开关, 而不是删掉那几行】本开关管着【两道执法】, 分在两个文件里
    //   (③ 是第三道, 但它【不归本开关管】):
    //   ① 钳位【位置】: RelayCore 的 4 个下发点 (clampToBoundaryActive 在 :871/:1262/:1328/:1363),
    //      由本开关管。⚠【姿态】的钳位 (clampOrientToBounds, RelayCore.cpp:49) 【不】走这个开关
    //      —— 它是独立的一处, 关掉本开关不会关掉它 (2026-09-22 核过)。
    //   ② 拒绝: SafetyPredictor 用【同一个函数】当谓词 (clamped 与 target 不同 ⇒ REJECT +
    //      ERR_SAFETY_BOUNDARY)。⚠ 这一道【不是夹, 是整条命令不执行】。
    //   ⚠★ 【实测核过: ② 今天也是死的】它那两个调用点 (:874 evaluate / :1143
    //     evaluatePositionOnly, 都是 RelayCore.cpp 的行号) 拿到的都是 :871【已经夹过】的值
    //     (tcpCheck 来自 servoCmdX/Y/Z, 而那三个就是从 clamped 抄的) ⇒ 谓词恒"没有越界"
    //     ⇒ 它从不拒绝。⚠ 例外【一处】: :1143 那条在【纯姿态模式】下拿到的值会被 ≤5mm 的
    //     tcpAdj 推到夹紧之后 ⇒ 开关一开它就会拒。逐行依据见 SafetyPredictor::evaluatePositionOnly
    //     里 2026-09-22 那段标注 (它与 :874 那条死的理由【不同】), 别只看这里。
    //     ⚠ 我一度断言"只删 ① 会被 ② 拦住"—— 那是【没核调用图就下的结论】, 已在同一处改回。
    //     记在这里是因为它与会话里那条教训同类: 结论要跟着读过的代码走, 不跟着"应该会这样"走。
    //   ⇒ ①② 两道【仍然一起关】, 但理由不是"否则拦得住", 而是【保持两者一致】: 本开关声称的是
    //     "①② 不再以任何方式拦", 而 ② 在调用图变化时 (有人传进一个未夹的目标) 就会活过来。
    //   ①② 两道由 SafetyBoundary::clampToBoundaryActive 这一个入口把关；
    //   ★ 姿态钳位是【第三处、独立的一处】(RelayCore.cpp:49 的 clampOrientToBounds, 唯一调用点
    //     RelayCore.cpp:1093), 它【不受本开关管】⇒ 本开关关着的时候, 姿态那一路【仍然在夹】。
    //   **别绕开这个入口直接调 clampToBoundary** (那个是纯函数, 给用例用的)。
    //
    // 【关掉之后少了什么 —— 如实记】
    //   · 目标位置可以被下到盒子外: X/Y/Z 的那个 [−300,250]/[−350,250]/[140,500] 不再生效。
    //     ⚠ Z 下界 140mm 是【落笔】最可能被挡的原因 —— 也就是这次要求关闭的直接动因。
    //   · 机械臂【没有任何工作空间边界保护】了: 撞到桌面/底座时不会有代码拦, 也不会有人告诉你。
    //     而且虚拟约束力早已关闭 (见 FORCE_CONSTRAINT_FORCES_ENABLED) ⇒ **靠近边界时也没有
    //     任何触觉提示** ⇒ 操作员只能靠视觉。
    //   · ⚠ 【仍然保留】的检查 (与这个盒子无关, 别以为关了就全没了):
    //       - SafetyPredictor 的 Z 范围 [0, 795mm] (那是机器人本体行程, 不是那个盒子);
    //       - 奇异区/自碰撞/报警点那几条, 各自有自己的门限。
    //
    // 【怎么恢复】改成 true 即可 —— ①② 两道执法同时回来, 不需要改别的文件。
    //   (③ 姿态钳位与此无关, 它【从未离开过】。)
    // ⚠ 这与用户 2026-09-21 关闭虚拟约束力是同一类决定 (为了让演示能跑而放弃一层保护),
    //   处置也照同一条规矩: **代码保留、做成开关、取消的原因就地写明**。
    const bool SAFETY_BOUNDARY_CLAMP_ENABLED = false;

    // ========== SafetyPredictor 安全预判参数 ==========
    const double WORKSPACE_RADIUS         = 620.0;   // CR3 最大工作半径 (mm)
    const double ROBOT_MAX_Z              = 795.0;   // CR3 总高度 (mm)
    const double SINGULARITY_COND_WARN    = 100.0;   // 雅可比条件数: 警告阈值
    const double SINGULARITY_COND_REJECT  = 500.0;   // 雅可比条件数: 拒绝阈值
    const double ALARM_DANGER_RADIUS      = 30.0;    // 历史报警点: 危险半径 (mm)
    const double ALARM_WARN_RADIUS        = 80.0;    // 历史报警点: 警告半径 (mm)

    // ========== 网络参数 ==========
    constexpr const char* ROBOT_IP = "192.168.101.11";
    const int ENABLE_PORT = 29999;
    const int MOTION_PORT = 30003;
    const int RECV_BUFFER_SIZE = 1024 * 64; // 64KB

    // MATLAB 中继站 GUI 连接
    constexpr const char* RELAY_IP = "127.0.0.1";
    const int RELAY_PORT = 8888;
    const int RELAY_UPDATE_INTERVAL = 33; // 更新间隔 (ms), ~30Hz

    // ========== 机械臂末端负载【种子值】(工具链: 传感器 + 笔夹 + 笔) ==========
    // 注意: 这三个量【只能实机测出来】, 正式数据源是 force/PayloadCalibration
    //       (payload_calib.json, 由 'm' 采集 + 's' 求解生成)。
    //       这里的值只是"还没标定过"时的兜底 —— 第一次启用机械臂前还没有任何
    //       实测数据, 而 EnableRobot 必须先给一个负载才能使能, 所以留一组估计值。
    // 估计来源: Hardware/tools/compute_payload.py (硬件改动后可重跑复核)
    // 实测方法: 启动客户端 → 'm' 采 6~8 个姿态 (笔尖悬空, 只改姿态) → 's' 求解
    //          → 自动重新下发 EnableRobot 并存 payload_calib.json → 再 'm' 复验
    // 负载设置不准 → 碰撞检测误触发 / 拖拽失控 / 30004 力值随姿态漂移 (见
    // Docs/机械臂资料/Dobot CR3机械臂参数文档.md §负载设置)。
    const double ROBOT_PAYLOAD_SEED_KG    = 0.66;   // 工具链总质量估计 (kg)
    const double ROBOT_PAYLOAD_SEED_CX_MM = 0.0;    // 质心偏心 X 估计 (mm)
    const double ROBOT_PAYLOAD_SEED_CY_MM = 0.0;    // 质心偏心 Y 估计 (mm)
    const double ROBOT_PAYLOAD_SEED_CZ_MM = 80.4;   // 质心偏心 Z 估计 (mm, 法兰下方)

    // 【已删: SIGN_PROBE_* 一组常量 (原 68~118 行)】
    // 它们服务于"下发两个候选负载、实测裁决 CZ 符号"的探针。实机实测(2026-09-18)
    // 证明那条路走不通: 机械臂内部负载从 TCP 口【改不动】—— EnableRobot / Payload /
    // LoadSwitch 三条通道全无响应 (0.25 kg 的变化只引起 0.0002 N·m 的读数变化)。
    // 而符号本来只在下发机械臂时才需要; 改成【本地补偿】后整条探针链路已移除,
    // 这几个常量随之作废。(保留记号, 免得后人又照着它们重建一套探针。)

    // ========== 机械臂运动参数 ==========
    const float SpeedL = 100;                // 运动速度比例 (1~100)
    const float MIN_DELTA_THRESHOLD = 1.0f;  // 最小位移阈值 (mm)
    const unsigned int CP_SMOOTH_RATIO = 100; // 平滑过渡比例 (0~100)

    // ========== 力传感器参数 ==========
    const int FORCE_REALTIME_PORT = 30004;       // 实时反馈端口 (125Hz)
    const int FORCE_EFFECTIVE_SAMPLE_RATE = 125; // 传感器数据采样率 (Hz)
    // 【力处理链的节流【下限】】RelayCore::pollForce 的入口节流: 两次处理【至少】隔这么久。
    // ⚠ 它【不是】实际节拍 —— 真实节拍还要加上主循环本身的耗时。2026-09-21 实测
    //   (用 force_demo_log.csv 的 t_ms 列): **46~203ms、均值 92~192ms, 而且【是变的】**。
    //   ⇒ 所以任何"按固定采样率换算"的常数都是错的。step() 内部的 dt 与在线零偏的 α
    //     都已改成【用实测耗时】(见 ForceCompensation 的 feedMotionEstimator / stepIntervalSec)。
    //   ⚠ 这个常数【现在只有一个消费者】: pollForce 的节流门。别再拿它当采样率用。
    const int FORCE_POLL_INTERVAL_MS = 33;       // 处理节流【下限】

    // 30004 帧自检 (TestValue @48 == 0x0123456789ABCDEF) 的档位。语义与理由见 robot/FrameLayout.h。
    //   0 = 关:  完全不做 (退回 2026-09-21 之前的行为)
    //   1 = 只报不判【默认】: 校验 + 首次报字节序 + 不匹配时响亮报警, 但【照旧收下这一帧】
    //   2 = 判:  不匹配就拒帧 + 断线重连 (把"安静地错位"变成"响亮地重连")
    // ⚠ 为什么默认 1 而不是 2: 我们【还没在真机上见过这个字段】—— 它可能没被填, 也可能
    //   不是我们以为的字节序。判错一档而猜反, 客户端会变成【永久重连循环】(那会挡掉现场工作)。
    //   ⇒ 先在真机上确认一次"自检通过 + 字节序是哪个", 再改 2。
    //   这与"闸门表先并排报 comp−@720、看清参考量之前不改判据"是同一套做法: 先观察, 后判决。
    const int FORCE_FRAME_MAGIC_MODE = 1;
    // ===== 滤波器的工作采样率与截止频率 (2026-09-21 重定) =====
    //
    // 【为什么有这一对, 而不是从前那个 `fs = cutoff × 4`】
    //   从前 ForcePipeline::init() 里写的是 `fs = FORCE_FILTER_CUTOFF * 4.0` (= 120 Hz) ——
    //   两个量【互相定义】, 所以它读起来像"设计成 fs/4 = 30Hz 的 Butterworth", 而真相是:
    //     · 那个 120 是【猜的】, 从没实测;
    //     · 而流水线当时跑在 ~11 Hz 上 (pollForce 实测间隔均值 92 ms) ⇒ 实际截止 ≈ 2.75 Hz,
    //       噪声只被削掉约 20% —— 实测: 原始 @1304 的 Fz sd 0.142 N ⇒ 落到日志里的
    //       `filtered` Fz sd 只有 0.115 N。
    //   ⇒ 现在两件事一起改了 (见 RelayCore::forceReaderThread): 补偿+滤波挪到【帧率】上跑。
    //     实测帧率 122.9 Hz (8.139 ms, 最大间隔 9.0 ms) —— 与厂商文档的 8 ms/125 Hz 对得上
    //     (Docs/机械臂资料/TCP_IP远程控制接口文档.md:63)。
    //
    // 【为什么 fs 要单列一个常数】它必须【等于流水线的真实调用率】, 否则 Butterworth 算的是
    //   归一化频率 fc/fs, 系数就整体错位 —— 这正是上面那笔账的来历。这里写文档值 125;
    //   实测 122.9 (差 1.7%, 对截止频率的影响可忽略)。**改了处理节拍就必须改它。**
    const int FORCE_FILTER_FS_HZ = 125;          // 流水线真实工作速率 (Hz) —— 必须等于实际调用率

    // Butterworth 截止频率 (Hz)。2026-09-21 从 30 改到 5 (原来那个 30 大得没有意义:
    // 它与当时假定的 fs=120 只差 4 倍, 等于几乎不过滤)。
    // 【5 Hz 这个数怎么来的 —— 一半是量出来的, 一半是取舍, 照实说】
    //   · 量出来的那一半: 帧率噪声探针 ('n') 实测, 原始 @1304 的 Fz 在【连续 k 帧取平均】后
    //     sd 的下降倍数是 k=8: 0.218、k=16: 0.153 (理论白噪声值 0.354/0.250 —— 实测比白噪声
    //     还低, 因为 lag1 自相关是 −0.54, 那是一个接近 Nyquist 的高频分量, 平均起来更好使)。
    //     取 k≈11 帧 (≈90 ms) 那一档 ⇒ 预期 sd ≈ 0.142 × 0.19 ≈ 0.027 N。
    //   · 取舍的那一半: 这个 k 同时也是【滞后】。90 ms 的等效平均 ⇒ 群延迟约 45 ms。
    //     幅频上, 2 阶 Butterworth 的等效噪声带宽 B_n ≈ 1.11·fc, 与 1/(2T) = 5.5 Hz 对齐 ⇒ fc ≈ 5 Hz。
    //   ⚠ 【这是一个起点, 不是一个定论】: 上面的换算用的是"等效噪声带宽对齐", 而真实噪声谱
    //     并不是白的。判据是【量出来的结果】: 改完重跑静止采集, 读 force_demo_log.csv 里
    //     filtered 的 Fz sd —— 目标是 < 0.03 N (改之前 0.115 N)。达不到就往下调 fc。
    const int FORCE_FILTER_CUTOFF = 5;           // Butterworth 截止频率 (Hz) —— 见上面那一大段
    const int FORCE_STALE_MS = 200;              // 数据超时阈值 (ms)
    const double FORCE_RESIDUAL_DEADZONE_N = 0.20; // 补偿后死区 (N) — 略高于运动噪声 0.17N
    // ⚠ 2026-09-21 记: 上面那句"略高于运动噪声 0.17N"是【旧噪声水平】下的依据, 现在不成立了。
    //   当时的处境: filtered sd ≈ 0.115 N ⇒ 死区只有 1.7σ ⇒ 实测 6.9% 的帧 |Fz|>0.2 N 直接穿过去,
    //   最大一趟折合到 Touch 上 1.18 N (现场感受: "方向飘动的阻力 / 把毛笔往下压")。
    //   改成帧率处理后噪声降下来 (目标 sd < 0.03 N), 0.20 N 会变成 >6σ ⇒ 静止时基本恒为 0。
    //   ★ 这个值本身【这一次没有】跟着改 —— 一次只动一件事; 等新噪声水平量出来再定它该是多少。

    // 启动零偏漂移检查的告警阈值 (N)。
    // 负载正确时残余力与姿态无关, 所以任意静止姿态下 "补偿后读数" 就是零偏漂移量。
    // 取 0.5 N: 明显高于死区 0.20 N 与噪声本底 (~0.05 N), 免得天天误报。
    const double FORCE_ZERO_DRIFT_WARN_N = 0.5;
    // 上面那个检查出结论所需的【最少样本数】(闸门放行后累计到的帧数)。
    // ⚠ 它【必须】只有这一处定义: 从前 main.cpp 里有一份、用例里又硬编码了同一个数,
    //   两边各改各的 —— 常量变了用例不会红, 而是【静默地换了含义】(那几条边界断言
    //   写的是"采到 9 个 / 至少 10 个", 数字一改它们就不再测原来那条边界)。
    //   现在调用侧与用例都读这一个常量, 并且用例显式钉住它的值 (改了就响亮地红)。
    const int FORCE_ZERO_DRIFT_MIN_SAMPLES = 10;

    const double FORCE_MAX_SENSOR_N = 200.0;     // 传感器量程 (N)
    const double FORCE_MAX_TOUCH_N = 3.3;        // Touch 最大安全力 (N)
    // 力反射增益 —— 【净比例 = (FORCE_MAX_TOUCH_N / FORCE_MAX_SENSOR_N) × 本增益 = 0.0165 × 本增益】
    //   即输出到 Touch 的力 = 传感器力 × 净比例 (逐轴, 且先过 FORCE_RESIDUAL_DEADZONE_N)。
    //
    // ★ 2026-09-21 从 5.0 改成 60.0 (净比例 0.0825 → 0.99, 约【1:1】)。依据与代价:
    //   · 原来的 5.0 是照"放大到可感知范围"定的, 而 ForcePipeline.cpp 的设计注释写明它假设的
    //     是【典型接触力 5~30 N】(净比例约 12:1)。**毛笔的笔压不在那个区间。**
    //   · 实测 (run-005 §7.6): 模拟写字的笔压只有 0.3~0.6 N, 主分量约 0.36 N。
    //     代进旧比例: 0.36 × 0.0825 = 0.030 N —— 只有 Touch 自身分辨率(~0.06 N)的【一半】
    //     ⇒ **摸不到**。净比例 ~0.99 之后 0.36 N → 0.36 N (约 6× 分辨率) ⇒ 清晰可感。
    //   ⚠ 代价【如实说】: 运动噪声同比例放大。实测运动噪声 sd = 0.11~0.16 N (run-005 §7.7,
    //     空中跟随/手不施力) ⇒ 改后手上会有约 0.15 N 的抖动。
    //     **SNR ≈ 2~5 不变**(同比例放大不改 SNR); 那个上限由传感器底噪(~0.1 N)与写字力
    //     (0.3~0.6 N)之比决定 —— 不换更硬的笔/写得更用力就压不下去。
    //   ⚠ 安全性不变: 力在 mapForceToTouch 里先 clamp 到 FORCE_MAX_TOUCH_N, hapticCallback
    //     里还有一道 ⇒ 净比例 ~1 时 3.3 N 的笔尖力即撞上限。
    // ★ 2026-09-21 现场再调: 60 → 120 (净比例 0.99 → **1.98**, 约 2:1)。
    //   用户要求"调大力传感器读数与 touch 受力的映射关系"——**手感上 1:1 还不够**。
    //   依据: 现场实测笔压约 1.15N ⇒ 1:1 时手上 1.14N(19× 分辨率) —— 理论上够,
    //     但**主观上仍不够醒目**, 所以再加一倍。
    //
    // ⚠ 三条代价, 都要记着:
    //   (1) **静止残余同样被放大**: 现场 comp_z 有一次到 −0.52N ⇒ 2:1 之下手上会是 −1.03N
    //       的常驻力 ⇒ **"静止时为 0"这条要求会明显变难** ⇒ 调零必须干净 (重新按 'z')。
    //       这是本增益最直接的对价: 放大信号的同时也放大残余。
    //   (2) **饱和点提前**: hapticCallback 里按 FORCE_MAX_TOUCH_N=3.3N 夹 ⇒ 净 1.98 之下
    //       传感器力到 3.3/1.98 ≈ **1.67N 就打顶** ⇒ 更大的笔压不再区分强弱 (但不会失控)。
    //   (3) 噪声 (0.11~0.16N) ⇒ 手上 0.22~0.32N ⇒ 会更明显地感到抖动。
    // ⚠ 这个数是**手感参数**, 只能现场定 ⇒ 觉得还不够就继续加, 太吵就回退。改它不影响任何判据。
    // ⚠★ 2026-09-22 起本值【不再是真值】, 只是【出厂默认 / 兜底值】。
    //   【优先级】calib/force_tuning.json  >  本值。
    //   运行时真值住在 ForceTuning (force/ForceTuning.h) —— 在 MATLAB 界面上可改、跨重启保留。
    //   ⇒ 改了本行却"没反应"是【正常】的: 去看启动横幅那一行 "[Tuning] 力反射增益 = ..., 来源: ..."。
    //   为什么这句必须写在这里而不只写在 ForceTuning.h 里: 下一个人会先来改这一行,
    //   而"改了没反应"正是本项目反复出现的"旧前提"形态。
    const double FORCE_REFLECTION_GAIN = 120.0;  // 力反射增益: 出厂默认 (净值 ≈2:1; 运行时真值见 ForceTuning)

    // ★★★ 横向两路的反馈符号 —— 2026-09-21 定为 **−1**。用户现场实测"阻力的方向都反了"。
    //
    // 【它是什么】Touch 系与机械臂工具系之间【整体的方向关系】。力映射应当是【位置映射的逆】:
    //   操作员把笔杆移动 Δp_t ⇒ 机械臂移动 M·Δp_t (M = relay/CoordinateTransform.h 的
    //   convertTouchToRobot, 当前无 calibration.json ⇒ 走硬编码那条:
    //   robot = (touch.x, −touch.z, touch.y))。
    //   摩擦反着笔的运动方向 ⇒ 要成为【阻力】, 手必须被推向 −Δp_t ⇒ **L·M = I ⇒ L = Mᵀ**。
    //   ⚠ 而代码里写死的那组轴对应【等于 Mᵀ 再整体取负】—— 即 L = −Mᵀ。
    //
    // 【σ = −1 这个整体符号从哪来 —— 只有一行有实测锚点, 而它把三行一起定了】
    //   垂直那一行是唯一被实测钉过的: 压笔 ⇒ comp_z = −1.55 N (─ 见 FORCE_FEEDBACK_Z_SIGN
    //   那三行原始数据), 而现场确认过 **`−fz` 的方向是对的** (用户当时只反对"顶得太狠、
    //   落不了笔", 没说方向反) ⇒ 那一行的整体符号是 −1。
    //   一个坐标系之间的关系没有理由逐轴各有一个不同的整体符号 (M 自己的逐轴翻转已经由 M 表达)
    //   ⇒ σ = −1 对三行同时成立 ⇒ 横向两路也应取 −1。
    //   ✅ 独立第二路支持: 用户现场报告"阻力的方向**都**反了"—— 若只有某一个轴错, 感觉会是
    //      "某个方向反了", 而不是"都"。两路证据同向。
    //
    // ⚠ 【这一条是推导+现场描述, 不是逐轴实测的】—— 与垂直那条的强度不同, 照实标出来。
    //   可证伪的检验: 把笔尖按住并朝某个方向推, 手上必须被推向【相反】方向。若仍是"顺着推",
    //   说明 σ 推断错了 ⇒ 那时改用【逐轴实测】(像垂直那次一样: 单轴施加已知外力, 读 comp 的符号)。
    // ⚠ 改它之前先读上面两条 —— 这一组符号 2026-09-21 当天翻过好几次, 每次都是因为引用了
    //   注释里的约定而不是实测的 comp 符号。
    const double FORCE_FEEDBACK_LATERAL_SIGN = -1.0;

    // ★★★ 垂直分量的反馈 —— 2026-09-21 最终定为 **0.0 (这一轴关掉)**。用户现场定案。
    //
    // 【为什么关, 而不是选个符号】垂直那一轴 (Touch Y) 就是操作员用来【把笔落到纸上、
    //   维持入纸深度】的那一轴 (位置映射里它对应笔的上下)。用户实测: **+Y 的力会把笔抬起来,
    //   于是根本落不了笔。** 而反方向的结论更早就有 (往下推 = 帮忙往纸里按)。
    //   ⇒ **两个方向都不行** ⇒ 不是符号问题, 是【这一轴不该承担反馈】。
    //     把"落笔的道具"和"反馈通道"叠在同一个自由度上, 任何显著力都会使写字无法进行。
    // 【那"阻力"从哪来】横向拖曳 —— 笔尖在纸面上拖, 摩擦反着运动方向, 那两路由
    //   hapticOut[0] (Fx→X) 与 hapticOut[2] (Fy→Z) 给, **本次未动**。
    //
    // 【各轴的量级, 供以后调"阻力大小"用 (用户: "阻力的大小再慢慢调")】
    //   净比例 = (3.3/200) × FORCE_REFLECTION_GAIN = 0.0165 × 120 ≈ 1.98 (逐轴, 先过死区)。
    //   实测"往下压"那次: comp_z = −1.55 N ⇒ 若这一轴开着, 手上会是 3.07 N (已经到 3.3N 夹子的 93%)。
    //   ⇒ 这个量级下无论往哪边都是"写字没法进行"的量级, 关掉是对的。
    //
    // 【若将来要重新打开这一轴 —— 必须同时满足三件事】
    //   ① 符号取 **−1**。依据是实测, 不是推理 (2026-09-21, 笔尖垂直压纸, 闸门 OK):
    //        raw @1304 z:  +4.63 (悬空) → +3.08 (压住)     Δ = −1.55 N
    //        comp 窗口均值 z (20 帧):                       −1.5516 N
    //        @576 z:       −0.05       → −1.606            Δ = −1.56 N   ← 独立第二路, 同号同量级
    //      ⇒ "纸面往上顶"在传感器系里是【负】z。
    //      ⚠ 这个位 2026-09-21 当天翻过两次, 而翻错的原因值得记: 原始代码是 `-fz`, 它旁边注释
    //        写的是 "When robot is pushed UP (+Fz), Touch pushes DOWN (-Y) to resist" ——
    //        **代码是对的, 那行解释是错的** (它把"往上顶"当成了 +Fz); 我照着那句解释"修"代码,
    //        于是把对的改错了。⇒ 修正号【不许引用注释里的约定】, 只许引用实测的 comp 符号。
    //   ② 量级要重新定: 上面那个 3.07 N 是"关掉"的直接理由, 所以重开时必须先给一个
    //      **远小于**它的乘性系数 (本常数就是乘在 fz 上的, 不限 ±1 —— 例如 −0.1 ⇒ 手上 0.31 N)。
    //   ③ 用例 test_force_pipeline 的 coord_transform【会红】—— 它钉的就是"这一轴为 0"。
    //      那是**故意的**: 重开必须有人来读这段注释, 而不是悄悄改一个常数。
    //
    // ⚠ 另一层【尚未验证】: 第 4 步的轴对应 (Fx→X / Fz→Y / Fy→Z) 从未被独立验证过。
    //   若实测是"力出现在【错的轴】上"(而不是方向反), 那是轴映射问题、与符号无关 ——
    //   实验: 笔尖【垂直】压纸, 看 comp 的三个分量落在哪个轴上, 再看手上被推向哪个轴。
    //   (本次那份数据: 压纸时 comp 只有 z 显著动 (x −0.32 / y +0.05 / z −1.55) ⇒ 轴对应没露破绽。)
    const double FORCE_FEEDBACK_Z_SIGN = 0.0;
    const double FORCE_GRADIENT_LIMIT = 50.0;    // 梯度限幅 (N/frame)

    // 力反射增益的【斜坡速率】(增益单位/秒)。2026-09-22 新增, 与 FORCE_GRADIENT_LIMIT 同类。
    // 【为什么要它】增益现在是【运行时】可拖的 (MATLAB 滑条 ⇒ ForceTuning), 一次拖动就能把
    //   目标值从默认值直接送到范围上界 ⇒ 不做斜坡, 手上力会在【一帧之内】翻上好几倍。
    //   ⚠ 增益的范围与默认值【本注释不复述数值】—— 它们各有唯一一份定义:
    //       范围: ForceTuning::GAIN_MIN / GAIN_MAX (force/ForceTuning.h);
    //       默认: Config::FORCE_REFLECTION_GAIN (本文件, 且只是兜底, 运行时真值在 ForceTuning)。
    //     这段从前写着"120 拖到 300"与"2.5 倍"—— 那两个数就是上面两个符号的【副本】,
    //     范围一改就静默过期 (而读者会把它当依据)。2026-09-22 Fix round 1 改成引符号。
    //   总夹那条一直在 (HapticCallback 里的 maxF = Config::FORCE_MAX_TOUCH_N, 逐轴夹),
    //   所以不会失控, 但那股突变是"没预备"的。
    //   ⇒ 斜坡把它摊开: 走完 GAIN_MIN→GAIN_MAX 的时长 = (GAIN_MAX − GAIN_MIN) / 本值,
    //     当前 ≈ 0.25 s。⚠ 0.25 s 是这个算式【此刻】的结果, 不是设计目标 —— 本值或范围一变它就变。
    // ⚠ 与 FORCE_GRADIENT_LIMIT 的分工: 那个作用在 filtered[] 上, 【管不到增益之后】;
    //   本常数作用在增益本身上。两者不重叠。
    // ⚠ 每帧步长 = 本值 / FORCE_FILTER_FS_HZ (实现见 ForcePipeline::step 第 5 步),
    //   而 FORCE_FILTER_FS_HZ 是【流水线真实调用率】这个前提 ⇒ 换了调用率, 斜坡的实际时长
    //   跟着变 (上面那个 0.25 s 就是在当前那个调用率下算出来的)。
    //   ⚠ 本处【故意不写行号、也不写 HapticCallback.cpp 的行号】: 给到文件 + 符号名即可,
    //     行号加一行注释就漂。本项目有成文教训: 常数与行号一样脆, 引用前回源头核。
    const double FORCE_GAIN_SLEW_PER_S = 800.0;

    const int FORCE_RECONNECT_INTERVAL = 2000;   // 断线重试间隔 (ms)

    // ========== 力传感器标定参数 ==========
    const double FORCE_CALIB_SPEED_FACTOR = 0.30;       // 标定期速度因子
    const double FORCE_CALIB_STILL_COLLECT_S = 2.0;      // 初始静止采集时间 (s)
    const double FORCE_CALIB_MOVE_TIMEOUT_S = 5.0;       // 单姿态移动超时 (s)
    const double FORCE_CALIB_SETTLE_TIME_S = 0.5;        // 姿态稳定等待 (s)
    const double FORCE_CALIB_SAMPLE_TIME_S = 0.5;        // 数据采集时间 (s)
    const double FORCE_CALIB_MAX_RESIDUAL_N = 0.3;       // 标定残差阈值 (N)
    const double FORCE_CALIB_POSE_ANGLE_DEG = 15.0;      // 标定姿态偏角 (度)
    const int    FORCE_CALIB_NUM_POSES = 6;               // 标定姿态数

    // ★★ 2026-09-22 (I1): 实测 dt 带出来的两个【合理性守卫】。都不是采样率 —— 别当采样率用。
    //  FORCE_CALIB_MAX_INTERVAL_S: 两次轮询之间的间隔超过它 ⇒ 当作【没测到】(dt = 0),
    //    而不是把它当成真的过了这么久。理由: TARE 的计时器一步跨过
    //    SETTLE+COLLECT 就会用【单样本】定稿并把零偏【落盘】(见 finalizeBias 的 n)。
    //    实测正常间隔 46~203ms, 它取 1.0s 是留了 ~5 倍余量。
    const double FORCE_CALIB_MAX_INTERVAL_S = 1.0;
    //  FORCE_CALIB_MIN_TARE_SAMPLES: 定稿前至少要收到这么多帧。
    //    真实节拍 (~11Hz) 在 2.0s 的采集窗里应收到 ~22 帧 ⇒ 5 是"明显坏了"的下限,
    //    不是质量门槛。不足就不定稿、继续收 (下一帧再判), 并【响亮地】说一次原因。
    const int FORCE_CALIB_MIN_TARE_SAMPLES = 5;

    // ========== 力传感器安装偏转角 (重力模型) ==========
    // 力传感器相对法兰的安装偏转角 (度, 绕【工具 z】)。传感器实际测量的坐标系与 GetPose
    // 的 RPY 所描述的那个之间差这么一个角。
    // ⚠ 现在【只是种子/回退值】, 不再是真值: 真值由负载标定扫出来 (PayloadCalibration::solve),
    //   随 payload_calib.json 的 "sensor_yaw_deg" 持久化, 启动时经 TcpCalibration::setSensorYawDeg
    //   装上。三组实机数据都指向 ≈79–81.5°, 而 90° 与 80° 正好跨过 0.30 N 的验收门限 ——
    //   所以这个数不能再靠手改。以下的 90° 只在这些情况下生效: 没有标定文件 / 旧文件里没有该字段。
    // 消费者仍是唯一一份实现: TcpCalibration::gravitySensorFrame (力补偿与负载求解【共用】)。
    const double SENSOR_MOUNT_YAW_DEG = 90.0;

    // 连续多少次"结果不合理"就打红字错误并停止接受求解。
    // 防的是操作者反复按 's' 却每次被拒的空转 —— 那种情况下问题在硬件/采集, 不在求解器。
    const int CALIB_MAX_CONSECUTIVE_FAILS = 3;

    // ========== 力补偿运行时参数 ==========
    const double FORCE_MOTION_VEL_THRESH_MS = 0.002;      // 静止判定: 速度阈值 (m/s)
    const double FORCE_MOTION_ACC_THRESH_MSS = 0.005;     // 静止判定: 加速度阈值 (m/s²)

    // ★★★ 惯量项在补偿式里的【标度】—— 2026-09-21 新增, 取值 {-1, 0, +1} 都有意义。
    //   补偿式是: comp = @1304 − bF − A·g + FORCE_INERTIA_SIGN × (mass × acc)
    //     −1 ⇒ 等价于原来那句 `− Fi`（原行为）
    //      0 ⇒ 【不做惯量补偿】(这一项整个去掉)
    //     +1 ⇒ 把 mass×acc 加回去
    //
    // 【为什么它是"标度"而不是"符号" —— 因为推导的前提没能站住】
    //   我 2026-09-21 的推导是: @1304 − bF − A·g 里【已经带着 −m·a】(工具惯性),
    //   所以要剥出外力必须【加】回去 ⇒ 我把它改成 +1。
    //   ★ **实机复测否定了那个前提**: 改成 +1 之后现场"阻力变大" ⇒ 说明 @1304 里
    //     并不像我假设的那样含这一项 (或含的形式不同)。两种写法留下的残差分别是
    //     −m·a 与 +m·a —— **量级相同、方向相反**, 所以"变大"正说明推到了错的那一边。
    //   ⇒ 教训【可迁移】: **"@1304 里有没有工具惯性"这个前提没有被独立验证过**,
    //     而我在它上面做了一整串推导。本项目那条"写之前先回源头核前提"再次适用 ——
    //     这次的前提是"信号里含什么", 不是"代码怎么算"。
    //
    // 【★ 那次 +1 的实测【不作数】—— 当时坐标系是错的】
    //   我改 +1 之后现场"阻力变大", 于是把默认值退回 −1。**但那个测试是在【acc 还在基座系】
    //   的前提下做的** —— 方向都错了, 翻符号当然救不回来 (两个方向都是错的)。
    //   ⇒ 坐标系修好之后 (见 ForceCompensation.cpp 里把 acc 映到传感器系那一段), 符号要【重判】。
    // 【重判的依据】坐标系正确之后, 物理推导才能成立:
    //   @1304 − bF − A·g 里带着 −m·a(传感器系) ⇒ 要剥出外力必须【加】回去 ⇒ +1。
    //   ⇒ 所以默认值是 +1。若现场仍不对 (阻力反而变大), 再试 0 —— 0 有意义:
    //     它等于"不做惯量补偿", 而那能区分"符号错"与"这一项本来就多余"。
    // ⚠ 这一项的历史: 先是 −1(残余模型时代, 那时 mass 可带符号) ⇒ 全量模型下 mass 恒正 ⇒
    //   符号没了依据 ⇒ 又叠上坐标系错 ⇒ 两次实测都被污染。**现在两项都修了, 才算第一次真测。**
    // ★ 2026-09-21 定案: +1。判据不是"试出来哪个手感好", 而是【量的结论】:
    //   用 force_demo_log 的位姿列与 acc 列, 离线算了三种取值下 |comp| 随速度的变化:
    //       速度档       均速m/s   |comp0|   |comp+1|   |comp-1|
    //       静止          0.000    0.287     0.295     0.281
    //       慢(5-50mm/s)  0.027    0.307     0.310     0.304
    //       中(50-150)    0.090    0.317     0.322     0.313
    //       快(>150)      0.205    0.332     0.334     0.336
    //   ⇒ **速度涨 75 倍, |comp| 只涨 0.045 N** ⇒ 运动引起的力【小到在死区以下】;
    //     而且【三种取值彼此的差只有千分之几】 ⇒ **这一项在演示速度下无关紧要**,
    //     所以【这份数据判不出符号】(那需要一次大加速度的运动 —— 而它不值得做)。
    //   ⇒ 于是按【物理推导】取 +1: @1304 − bF − A·g 里带着 −m·a(传感器系),
    //     要剥出外力必须加回去。⚠ 坐标系已修 (acc 先映到传感器系, 与重力项同一变换)。
    //   ⚠ 历史: 这一项被两次实测污染过 (残余模型时代 mass 可带符号 ⇒ 全量模型下恒正;
    //     又叠上坐标系错)。**现在两项都修了, 而结论是"它不重要"** —— 别再在它上面花时间。
    //   ⚠ 现场另有一条相关观察 (用户): 用【示教器】控制时传感器读数也有类似变化 ——
    //     示教器绕过本客户端 ⇒ 那部分变化是物理的。但实测只有 ~0.045N ⇒ 不是手上那股"阻力"
    //     的来源; 那股阻力在 Touch 设备自身的机械阻力里 (在回路之外, 软件改不了)。
    const double FORCE_INERTIA_SIGN = +1.0;   // +1 = 加回去 (物理推导; 实测证明它无关紧要)
    // 运动估计器允许的最大喂样间隔 (s)。超过它就【重置】估计器, 不让二阶差分跨过一个坑。
    // 【为什么需要】喂样间隔是实测的 (见 ForceCompensation.cpp 的 feedMotionEstimator):
    //   实测 46~203ms 且【是变的】(主循环耗时), 偶尔还会卡得更久。跨过大坑的二阶差分
    //   量的是"两次采样之间的任意运动", 不是加速度 —— 那个数没有任何含义, 只能丢掉重来。
    //   取 0.5s = 实测最坏值(203ms)的 2.5 倍: 正常抖动碰不到它, 真卡顿才触发。
    const double FORCE_MOTION_MAX_GAP_S = 0.5;
    // 在线零偏 EMA 的【时间常数】(s), 仅静止态更新。⚠ 语义是时间常数, 不是"每帧更新率":
    //   每帧的 α 由它与【实测的每帧耗时】算出 (见 ForceCompensation::step 第 8 步) ——
    //   这样节拍怎么变, 时间常数都不会被悄悄改掉。
    //
    // 【为什么从 3.3 s 改成 600 s (2026-09-21, run-005 §7.4)】
    //   这个 EMA 把 bF 朝"comp → 0"拉, 而它【分不清】"传感器零点的慢漂"与"一个持续的外力"
    //   —— 对 EMA 来说两者是同一个信号。实测: 挂 1.9 N 重物 11 分钟, bF 吸收了 2.09 N
    //   (与载荷 2.076 N 差 0.5%), comp 归零、闸门一路放行 ⇒ 力被【静默擦除】。
    //   三个时间尺度摆在一起就看出原来的数错了:
    //     传感器零点漂移    0.4025 N / 19.5 h = 0.02 N/小时
    //     毛笔触纸的力变化   0.1 ~ 1 s
    //     旧的 EMA (α=0.01)  3.3 s   ← 与毛笔【同一个数量级】⇒ 把秒级的力也当漂移吃掉了
    //   600 s 下: 漂移跟踪滞后 = 0.02 N/h × 600 s = 0.003 N (比死区 0.20 N 小 60 倍),
    //   而 1 s 的力基本不动 ⇒ 两者差 ~600 倍, 分得开。
    //   ⚠ 【不要】改成"关掉": 传感器自身漂移 0.4 N/session 是死区 0.20 N 的 2 倍 ——
    //     关掉换来的是常驻的幽灵力 + 在死区边界上抖动。放慢 ≠ 关掉。
    //   ⚠ 0.02 N/小时 是【一个数据点】推出来的。要定准 τ 应先实测它 —— 'm' 模式的 SPACE
    //     会打印 bF, 开机记一次、一小时后记一次即可。若实测大得多, 这个数要跟着改。
    const double FORCE_BIAS_EMA_TAU_S = 600.0;
    const double FORCE_ACC_FILTER_CUTOFF_HZ = 10.0;       // 加速度估计低通截止 (Hz)

    // ========== 运行时一致性闸门 (2026-09-19) ==========
    // 逐通道比较【本地全量模型的输出 compensated】与【机械臂自报的参考量】。
    // ⚠ 判决【不是逐通道的全集】: 只有【投票通道】的差进判决, 其余通道照报不判 ——
    //   掩码的唯一一份实现是 ForceCompensation.cpp 的 g_guardVote (本文件下方与
    //   ForceCompensation.h 都指向那一处, 不在这里复述是哪几个)。
    // 在投票通道上: 两个模型都对时它们估计的是同一个量 (外力), 所以应当一致;
    // 不一致 ⇒ 至少一个错 ⇒ 拒绝把数据往下传 (compensated 置零)。
    // ⚠ 参考量【是哪一路】不在这里写死: 它由 ForceCompensation.cpp 的 guardReferenceValue
    //   一处定义 (那里也有"为什么是那一路"的实测出处)。换参考量就改那一处。
    //   @576 (fd.raw) 是同屏并排报出的诊断侧, 【只报不判】—— 下面的容差不比它。
    //
    // 容差【由实测导出】, 不是猜的。它约束的量是【compensated − 判据参考量】—— "判据参考量"
    // 是哪一路由 ForceCompensation.cpp 的 guardReferenceValue 一处定义, 所以这里【不写通道号】:
    // 一写死, 下一次换参考量就会留下一句假话 (这段文字已经吃过一次这个亏)。
    //
    // 力通道的容差 = 两个【来源不同】的合法误差项之和。两项都是"即使全都做对, 这个比较也会
    // 显示出来的差异", 所以容差必须把它们的合并盖住; 合并规则与它的代价写在 tol_F 那几行。
    //
    // (甲) 本地失拟 + 机械臂那一侧的模型类差:
    //      推导见 .superpowers/sdd/runtime-guard-report.md; 全部数字由
    //      tests/test_payload_calibration.cpp 的 test_runtime_consistency_guard_replay
    //      在四份夹具上现算并打印, 那条用例同时断言"容差 > 这个量级"。
    //      ⚠ 2026-09-21 起这件事由该用例的【前置遍历】承担 (逐姿态重放那一半在第一份夹具的
    //        第一个姿态就按设计红着 return, 所以留在那里的话只有第一份会跑到这条断言 ——
    //        而第一份恰好是四份里要求最低的)。前置遍历四份全跑, 断言按【最坏的那一份】判。
    //   eps_甲 = rmsForceN + (σ1−σ3)/2 · 9.81
    //         四份夹具上 = 0.15314 / 0.25195 / 0.28710 / 0.17591 N, 最坏 0.28710 N (15:30);
    //         · rmsForceN 是本地模型自己的失拟 (夹具实测量: 0.0224/0.0186/0.0191/0.0151 N);
    //         · 第二项是【机械臂那一侧】的模型类差: 它的力模型是"标量质量 × 正交"(m·Q),
    //           而本地 A 是自由 3×3。A 到【最近的】m·Q 的算子范数距离【恰好】是 (σ1−σ3)/2
    //           (在 m = (σ1+σ3)/2 处取到), 四份实测 = 0.013328/0.023784/0.027323/0.016391 kg
    //           (= 0.1308/0.2333/0.2680/0.1608 N 的重力响应), 这一份它【结构上表达不出来】。
    //           ⚠ 2026-09-19 复审改口径: 从前这里写的是 max|σ(A) − σ̄|。那个量【不是下界,
    //             是上界】—— 它恒 ≥ (σ1−σ3)/2 (四份实测大 0.12% ~ 14.9%)。用上界当依据会把
    //             "容差是合法差的几倍"说小, 而旧注释还称它"fail-closed 的方向": 方向正好
    //             说反 (依据取大了, 容差只会更松)。现在用的是精确距离, 所以下面那个倍数
    //             就是【真正的余量】, 不再有第二层水分。那次改口径时容差的【数值】一个都没动。
    //
    // (乙) 参考量【自带】的力偏置, 以及它跨轮次的漂移  (2026-09-21 加入):
    //      出处: Docs/superpowers/specs/2026-09-21-gate-reference-prereq.md
    //      §3.3 / **§3.3.1 (行集 + 算式 —— 第三方照它可逐位复现)** / §3.4;
    //      原始数据: 三段带力参考量那一列的采集 —— 引它的【冻结快照】
    //      Touch_Client/tests/fixtures/calib_poses_2026-09-20_frozen.txt
    //      ⚠ 不引 CalibStore 运行时可写目录下的那份活文件: 它在那里【只追加、永不截断】,
    //        每次标定采集都会改它 ⇒ 被本段引用的数字会随手漂 (2026-09-21 Task 8 冻的快照)。
    //      (三轮, 每轮 10 行 = 5 对原地复采)。逐轮把该通道的力对重力方向做
    //      "常数 + gx,gy,gz" 的回归, 取截距 c (三轮的三维条件数 22.3/16.7/18.8, 不是病态):
    //        forceOffsetMax_N   = 0.1548 N = max over 轮次 of max_axis |c|
    //                                       (21:58:46 轮的 c_y = −0.154790)
    //        forceOffsetDrift_N = 0.1813 N = max over 轴 of (max c_a − min c_a)
    //                                       【该轴跨轮的带符号极差】
    //        eps_乙 = 0.1548 + 0.1813 = 0.3361 N
    //      ⚠ 归约维度必须写明 —— 计划给的算式没定义它 (该文 §3.1): Drift 取【按轴、带符号的
    //        跨轮极差】这一读法 (x/y/z = 0.1434/0.1813/0.0915 ⇒ 0.1813); "先逐轴取 |c| 再跨轮
    //        相减"那条【次自然】读法给 0.1389 ⇒ 0.2937 (更小), 不采用 (该文 §3.4 两行都在)。
    //      ⚠ eps_乙 把同一个量算了两次 —— 这是【有意保守】, 不是算错: Max 与 Drift 都用到
    //        21:58:46 轮的 c_y: 带符号读法下 Max = |min c_y|、Drift 的下端就是 min c_y,
    //        所以 eps_乙 = max c_y + 2·|min c_y| = 0.0265 + 2×0.1548 = 0.3361。
    //      ⚠ 它【为什么单独一项】: (甲) 是我们自己模型的误差, (乙) 是【尺子自己】的偏置与它的
    //        不稳定性。参考量的力偏置会漂 (这就是 eps_乙 存在的理由), 所以不能并进 (甲)。
    //
    // 合并: eps_F = eps_甲 + eps_乙 = 0.2871 + 0.3361 = 0.6232 N ⇒ tol_F = 2 × 0.6232 = 1.2464 N。
    //   ⚠ 舍入约定 (只有一条, 免得读者看到两个规矩): 每一步都用【本文件印出的那一位小数】
    //     参与下一步运算 —— 即先按 4 位小数取 eps_甲 / eps_乙, 相加得 0.6232, 再乘 2。
    //     若改用未舍入的 0.287098 + 0.336054 = 0.623152 直接乘 2 会得 1.2463, 本文件【不用】
    //     那一条 (两条路只差 1e-4, 但混着写会显得自相矛盾)。落盘的数是 1.2464。
    //   ⚠ 取【正好 2 倍】, 不是"取个整数": 余量正好落在判据 (≥ 2) 上, 于是这是【满足判据的最小
    //     容差】—— 再取大就是白白少抓一截 (容差越大越抓不住东西, 见下面那一段)。所以这个数
    //     是算出来的, 不是凑的; 它不回退成"调到能过为止"的另一个理由是: 0.6232 这一侧是
    //     实测的两项, 与"闸门放不放行"无关 —— 它不来自它自己要检验的量。
    // ⚠ 合并规则取【最坏情形相加】, 这是【判断】, 不是从测量里导出来的 (要说清楚):
    //   两组测量没有给出联合分布, 所以"两者独立"这个前提【没有依据】—— 而它们【可能部分重叠】
    //   (机械臂那一侧的模型类残差, 正好也是参考量偏置的一个可能来源; 该文没有判它, 手上只有
    //   三轮、两个日期)。测量的现状也【不足以】证明相加不会过头: 三轮数据上【直接】量到的
    //   |compensated − 参考量| 逐姿态最大值 (只算投票的 Fx/Fy) 只有 0.1775 N —— 比 0.6232 这个
    //   界【小 3.5 倍】。⇒ 取【保守 (较大) 的那一边】: 相加。代价写下来: 平方和给
    //   √(0.2871² + 0.3361²) = 0.4420 ⇒ tol_F = 0.8840 N; 相加比它宽 0.3624 N (1.41 倍),
    //   折算成负载质量误差是 0.127 kg 而不是 0.090 kg。
    //
    // ★ 上调到 1.2464 N 之后它【还能抓住什么】—— 用户约束是"门限可以适当降低标准, 但安全起见
    //   不符合时还是要拒绝", 所以这一段是硬要求, 不是注解。闸门在
    //   |EMA(compensated − 判据参考量)| > 该通道的容差时拒绝那一路。当前投票掩码【只有
    //   Fx/Fy 两个通道】(掩码的唯一定义见 ForceCompensation.cpp 的 g_guardVote):
    //   Fz 与 Mx/My/Mz 都【照报不判】—— 力矩三个分量自 2026-09-21 起不再投票 (实测它们与
    //   参考量之间不存在"一致"态, 理由与代价见 g_guardVote 段), 而 tol_M 因此在【判决里】
    //   不再被消费: 它仍然印在逐通道表上作为该通道的读数尺度, 但没有任何通道按它投票。
    //   ⚠ 所以由容差 1.2464 N 导出的那条灵敏度【只覆盖 Fx/Fy 两个力方向】; z 方向没有闸门
    //     (见下面那段), 力矩方向现在也不再有任何判决意义。
    //   · 相对上面那个【界】的余量 = 2.00x; 相对【三轮实测到的最大合法差 0.1775 N】= 7.0x。
    //   · 折算成【负载质量误差】: 臂侧负载与本地模型差 Δm 会在重力方向上产生 Δm·9.81 的力,
    //     取最不利取向 ⇒ 门限 Δm = 1.2464 / 9.81 = 0.127 kg
    //     (= 工具链实测质量 0.4159 kg 的 31%)。
    //   · 【已知的那个失效模式仍被抓住】: 机械臂还拿着种子负载 0.66 kg 而实测是 0.4159 kg
    //     ⇒ Δm = 0.244 kg ⇒ 最不利取向下 2.39 N > tol_F, 余量 1.92x。⇒ 这种情况仍然【拒绝】。
    //   · 【代价】: 旧 tol_F = 0.50 N 对应的门限是 Δm = 0.051 kg ⇒ 同一条灵敏度掉了 2.49 倍
    //     (要 2.49 倍大的质量误差才触发)。0.90 N 量级的持续不一致过去会拒, 现在不会了。
    //   · ⚠ 上面全是【静止】口径的灵敏度 (量的是"模型错多少会被拒"), 【不是】"外力多少会被拒":
    //     换参考量后闸门在真实外力下的行为【没有任何仓库文件验证过】(该文 §4.2①, 遗留风险)。
    //
    //   eps_M = rmsMomentNm + |c_s|·(σ1−σ3)/2·9.81; 四份 = 0.00853 / 0.01400 / 0.01626 / 0.01068,
    //         最坏 0.01626 N·m (15:30 = rms 0.0012 + |c_s| 0.0560 × 类差 0.2680)。
    //   tol_M = 0.03 N·m = 1.85 × 0.01626 (逐份的比值 1.85~3.52)。
    // ⚠ 首版容差, 【必须在实机上复验】: 机械臂那一侧的真值要等正确的负载下发
    //   (Task 8) 之后才量得到。力通道的推导现在覆盖两项 [(甲)+(乙), 见上];
    //   力矩通道的推导【仍然只覆盖了"模型类不同"这一项】—— 若【判据参考量】的力矩参考点
    //   与传感器原点不同, 还会多出一项 |Δc × F|
    //   (Δc ≤ 14.15 mm, 见 Docs/superpowers/plans/2026-09-19-raw-channel-calibration.md:226;
    //    |F| ≤ 3.0932 N, 四份夹具 36 个姿态里力模的最大值, 出现在 12:38 pose 4 ——
    //    夹具的力列有【两路】(F576 与 F1304), 缺的是【判据参考量】那一路, 所以这个界
    //    是在【旧参考量】上量的)
    //   —— 那一项最大 14.15 mm × 3.0932 N = 0.0438 N·m, 比 tol_M 还大。
    //   若它是真的, 力矩通道会在 Task 8 之后【永远拒绝】。测到之后再复查, 不要凭猜放宽。
    //   ⚠ 这一条是【在旧参考量上写的假设】, 换参考量之后【没有复测】: 它现在的状态是"未决",
    //     不是"已验"。本段不动力矩容差的数值。
    //   ⚠ 2026-09-21 起力矩三个分量【不再投票】(见 ForceCompensation.cpp 的 g_guardVote 段),
    //     所以"若它是真的, 力矩通道会在 Task 8 之后永远拒绝"这条风险【不再由闸门承担】——
    //     它已经不可能让闸门拒绝了。tol_M 这个数【保留原值】: 它是该通道读数的尺度, 仍然
    //     印在逐通道表上, 只是不再有通道按它投票。
    //
    // ⚠⚠ 【z 力方向实际上没有闸门 —— 给出尺寸, 不要只说"少一道闸门"】(复审 Important 2)。
    //   Fz 不投票 (理由见 ForceCompensation.cpp 的 g_guardVote), 而力矩通道【兜不住它】——
    //   2026-09-21 起它【连票都不投了】(实测力矩与参考量之间没有"一致"态), 所以下面这个
    //   "力矩门若要看见 z 差得多大"的算法【现在是一道不存在的门】:
    //   z 上的模型误差 ΔFz 要经 c_s 叉乘成力矩误差才可能被看见, 量级 = |c_s_横向| · ΔFz。
    //   四份实测 |c_s_横向| 只有【0.47 ~ 0.78 mm】(横向 = sqrt(cs_x²+cs_y²), 由上面那条用例
    //   现算打印), 而 tol_M = 0.03 N·m ⇒ ΔFz 要到 tol_M / |c_s_横向| ≈ 【38 ~ 64 N】
    //   才有机会把力矩顶超限。也就是说: 几十牛量级的 z 力模型错误, 这一版闸门看不见。
    //   这是本次改动【已知的最大一处漏洞】, 明确作为【未决项】交给用户
    //   (补一个 z 的独立判据, 还是接受这个洞) —— 不是靠调容差能解决的。
    //   ⇒ 要补这个洞只能【新增一道独立的 z 判据】, 不能靠"把力矩的票加回来":
    //     力矩不投票是实测结论 (它连同姿态的重复性都不够), 复审判定"Fz 不投票"这个取舍
    //     本身可接受, 错的是没把代价说出来 —— 两件事都不因本次改动而变化。
    // 力通道 (x/y) 一致性容差 (N)。它约束的是【compensated − 判据参考量】这一条差, 参考量由
    // guardReferenceValue 一处定义 ⇒ 这里【不写通道号】。依据 (甲)(乙) 两项之和 = 0.6232 N,
    // 取 2 倍 ⇒ 1.2464 N (余量 2.00x), 以及它【还能抓住什么】, 都写在上面那一段里。
    const double FORCE_GUARD_TOL_FORCE_N   = 1.2464;  // N
    const double FORCE_GUARD_TOL_MOMENT_NM = 0.03;   // 力矩通道一致性容差 (N·m)
    // 逐通道差的 EMA 的【时间常数 (s)】—— 用"秒"定义, 不用"每帧 α"。
    //   意图不变: 等效平均 ~3.3 s, 把逐帧噪声 (夹具实测力 x/y 0.04~0.06 N, 力矩 0.002~0.006 N·m)
    //   压到容差之下一个量级以上。α 由【本帧实测耗时】现算 (α = stepDt/τ), 见 step() 的 7c。
    //
    // ⚠★ 2026-09-21 为什么从 `FORCE_GUARD_EMA_ALPHA = 0.02 (每帧)` 改成这个:
    //   那个常数是【每帧】的, 所以它隐含了一个采样率 —— 而 α 的注释当时按 30Hz 写的
    //   (FORCE_POLL_INTERVAL_MS 那个名义节拍), 实际却跑在 ~11 Hz 上。当天把补偿+滤波
    //   挪到【帧率 123 Hz】之后, 同一个 α 意味着:
    //       11 Hz 时  τ ≈ 0.092/0.0202 ≈ 4.6 s
    //      123 Hz 时  τ ≈ 0.0081/0.0202 ≈ 0.40 s      ← 快了 11 倍
    //   ⇒ 闸门对"本地模型 − 参考量"那个差的反应突然快了 11 倍 ⇒ 差在容差附近时它会【翻来翻去】
    //     ⇒ 被拒的帧 compensated 被置零 ⇒ 手上的力一开一关。**这是那次改动带出来的回归。**
    //   ⚠ 与 FORCE_BIAS_EMA_TAU_S 是同一类修法 (那条早就按实测 dt 换了, 这条漏了)。
    //     本文件里【不该再出现任何每帧固定的 α】—— 只要它隐含了采样率, 换节拍就会静默改时间常数。
    // ⚠ 取 3.3 s 是照【原注释写下的意图】, 不是照它实际跑出来的 4.6 s: 意图是设计点,
    //   4.6 s 是"名义节拍与实际不符"这个错误的产物, 不该把它固化下来。
    const double FORCE_GUARD_EMA_TAU_S = 3.3;
    // 同一条错误最快多久重报一次 (ms)。状态一变立刻报, 不变的按这个间隔复报。
    const int    FORCE_GUARD_REPORT_MS = 5000;

    // ========== 姿态控制参数 ==========
    const double ORIENT_MAX_STEP_DEG = 3.0;          // 单步最大角度增量 (degrees)
    // 姿态回路的【逐轴】响应门限 (degrees)。
    //
    // 【现状 —— 参照式, 2026-09-21】那条回路已改成:
    //     目标 = 按下按钮2时的机器人姿态 + R×(K × (笔杆现在 − 笔杆按下时))
    //   本参数只是"笔杆要转多少机器人才开始跟"的【响应门限】, 逐轴判。
    //   ★★ **它不再是防漂的关键参数** —— 漂移的根源是"把增量累加"这件事【本身】,
    //      不是抖动多大: 累加式下任何非零抖动都会被积进去, 所以死区调多大都只能把漂移
    //     调慢 (用户 2026-09-21 指出, 而且他说得对: 手抖永远非零、还因人而异,
    //     所以"去量化抖动"既不可靠也不该做)。改成参照式之后, 抖动只让目标在参照附近
    //     【颤动】, 手一回它自己就回去 ⇒ 不漂。
    //   ⇒ 因此【不需要】按每个人的手抖去标定它; 取一个"感觉上刚好不抖"的值即可。
    //
    // 【历史, 供理解为什么要改成参照式】
    //   · 它曾是"三轴 OR 的门": 任一轴 >= 本值 ⇒ 三轴【全部原样放行】(含另两轴纯噪声);
    //   · 而目标又是【无界累加】⇒ 抖动被放行后积分进目标、没有回中。
    //   ⇒ 现场表现: 移动手写笔时笔杆朝向只差 0.9°, 机器人姿态却转了 10.4°;
    //     缓慢移动也大幅晃动、范围越走越大。
    const double ORIENT_DEADZONE_DEG = 0.05;         // 姿态响应的【逐轴】门限 (degrees)
    const double ORIENT_GAIN = 1.0;                  // 姿态增益 (可调灵敏度)

    // ★★★ 姿态增量的【逐轴符号】(每个取 +1 或 -1) —— 2026-09-21 新增, 用来修"按钮2 转向反了"。
    //
    // 【原来是什么】RelayCore 里对三个轴做了一次【无条件的取负】:
    //     drx = -drx; dry = -dry; drz = -drz;
    //   注释写的理由是: "Touch Euler (ZYX intrinsic) 沿正轴看逆时针增大, 而 Dobot RPY 相反"。
    //
    // 【那个理由与仓库里的两份实现都不符】
    //   · Touch 侧: stylusOrient = 设备变换的 ZYX 内旋 Euler (R = Rz·Ry·Rx) ——
    //     见 HapticCallback 的提取 (sy = -R[2][0]; rx = atan2(R[2][1], R[2][2]);
    //     rz = atan2(R[1][0], R[0][0])) —— 这正是 Rz·Ry·Rx 的标准提取式。
    //   · Dobot 侧: RPY 由 TcpCalibration::rpyToMatrix 建 R = Rz·Ry·Rx
    //     (R[6] = -sry; R[7] = cry·srx; R[8] = cry·crx) —— **同一个约定**。
    //   ⇒ 两边同源 ⇒ 同一个角度增量应当产生【同向】的旋转 ⇒ **不该再取负**。
    //   ⇒ 现场实测相符: 绕一个轴转手写笔, 机械臂往【反】方向转 (2026-09-21)。
    //
    // ⇒ 默认全部 +1 (即不翻转)。⚠ 保留成可配置而【不是】直接删掉那次取负, 是因为还有另一种
    //   可能: "只有某些轴反" —— 那不是这里的事, 而是下面那段【轴重映射】的事
    //   (relay/RelayCore.cpp 里, Calibration::enabled 为假时用的硬编码置换
    //    robot_X=touch_X / robot_Y=-touch_Z / robot_Z=touch_Y 从未被验证过)。
    //   ⚠ 若实测是"某个轴反、另一个轴向错", 请把【对调的轴】记下来 —— 那是置换问题。
    const double ORIENT_FLIP_RX = +1.0;
    const double ORIENT_FLIP_RY = +1.0;
    const double ORIENT_FLIP_RZ = +1.0;
    // ★★ 2026-09-22 实验开关 —— 修"姿态接缝抖动"（现场已定性确认；翻回 false 即恢复原行为）
    //
    // 【现象 + 现场证据】按住按钮2 朝 **+rx** 方向滚过 ±180 时，机械臂在【该点附近抖动】；
    //   同一姿态往 **−rx** 反向则【不抖】（现场 2026-09-22 实测，判别性）
    //   ⇒ 因果是这条钳位。
    //   控制台同时打出 `[Safety] Orientation target out of bounds, clamped. Original: (183,-2.76643,15.5627)`
    //   —— 而 `183 − 180 = 3.0` **恰等于 `ORIENT_MAX_STEP_DEG`** ⇒ `m_targetOrient.x` 已顶在 180、
    //   每帧被夹回 ⇒ 手的抖动让目标在"被夹到 180"与"落回 180 以下"之间**逐帧来回** ⇒ 指令以帧率往复。
    //
    // 【根因】`m_targetOrient` **从不 wrap**，而钳位比的是**绝对值** ±180。
    //   ⚠ 而 ±180 只是 **RPY 表示的接缝**，**不是机械臂的危险姿态** ——
    //   J6 量程 ±360（`Docs/机械臂资料/Dobot CR3机械臂参数文档.md` 各关节运动范围）。
    //   ⚠ 顺带记账：下面这三个 `SAFE_R*` 的注释只有"安全限位"四个字，**没有出处、没有推导** ——
    //   它们看起来就是照角度满量程抄的。**"该是多少"这个问题【未解决】**，见计划待办。
    //
    // 【修法（两处，见 RelayCore.cpp）】
    //   (a) 钳位改成比【相对按下按钮2 那一刻参照的偏移】—— 那才贴合"别相对起始姿态转太多"的意图；
    //   (b) 下发前把三个角**规范化到 [−180,180)** ⇒ 目标可以连续滚过接缝，不再撞墙。
    //   ⚠ 不需要"最短路径角差"：目标**不 wrap**、`desired` 也是连续量 ⇒ `desired − target` 本来就是小数。
    //
    // ⚠⚠ 【未验证的前提 —— 这就是它为什么是"实验"】控制器怎么处理 `180 → −179.9` 这种**数值跳**？
    //   仓库里**没有依据**：厂商文档只给了**关节**范围，**笛卡尔姿态范围一个字没有**；
    //   而 `force_demo_log.csv` 里机器人**报出来的** `pose_rx` **从未越过 ±180**
    //   （两端各约 1° 内共 1.4 万行）⇒ 提示 ±180 是真的接缝。
    //   · 若控制器按**矩阵**做 IK ⇒ 两种写法同解，无事；
    //   · 若它按 RPY **直接解释** ⇒ 会命令 J6 转**一整圈**（现场一眼看得出）。
    //   ⇒ **现场一跑就知道；不对就把本开关翻回 false 并重建**（回滚 = 一行）。
    // ⛔⛔ 2026-09-22 现场结果：**这条修法是错的，已关闭** —— 那个"未验证前提"有了答案，
    //   而且是坏的那一支：**控制器【不】把 RPY 当模 360**。
    //   现场（`ORIENT_SEAM_FIX_ENABLED = true` 的那版 exe，18:53 构建）：
    //     用户报告 —— "接缝**急速抖动**，导致**关节超速报错**"。
    //   ⇒ 机制：本修法让 `m_targetOrient` 可以连续越过 ±180，而「下发前规范化」把它从
    //     `180` 跳到 `−179.9` ⇒ 控制器**把这个数值跳照字面解释**（命令 J6 转一整圈）
    //     ⇒ 需要极高的关节速度 ⇒ **关节超速报警**。
    //   ⇒ **⇒ 结论：(A) 环绕/规范化那条路在本控制器上【不可行】。** 姿态实现必须
    //     **避免穿过接缝**，而不是穿过去再表示回来。
    //   ⚠ 保留这个开关（而不是删掉代码）是刻意的：它记着"为什么不能这么做"，
    //     而那条结论不是从文档推的，是**实测撞出来的**。
    //   ⚠ 那三个 `SAFE_R*` 没有出处的问题**仍然未解决**（见下面的记账）。
    const bool   ORIENT_SEAM_FIX_ENABLED = false;
    const double ORIENT_MAX_OFFSET_DEG   = 150.0;   // 姿态相对参照的最大偏移 (deg)

    // ★★ 2026-09-22 实验之二 —— 修"按钮2 平移时抖"（现场实测：笔杆姿态摆幅 ≤ **1°**）
    //
    // 【现象 + 为什么是平移】按住按钮2（末端**只转不平移**）时，**手做平移**就会出现抖动。
    //   现场实测：平移过程中笔杆姿态在 **1° 以内**摆动（那是手不可避免的微动，不是操作意图）。
    //
    // 【机制】被判门限的量是"**相对按下按钮2 那一刻的绝对偏移**"（RelayCore 里 `offx/offy/offz`），
    //   而门限 `ORIENT_DEADZONE_DEG = 0.05°` —— **比手抖小 20 倍** ⇒ 偏移在门限两侧**来回穿**
    //   ⇒ `drx/dry/drz` 在"原样放行"和"置 0"之间**逐帧闪** ⇒ 姿态指令以帧率往复 ⇒ 臂抖。
    //   **⚠ 全链路上【没有任何滤波】**：`HapticCallback` 把 `HD_CURRENT_TRANSFORM` 解出的欧拉角
    //   原样写进 `appState.stylusOrient`，`RelayCore` 原样读出来。
    //
    // 【为什么不用"把门限抬到 1° 以上"这条路】门限作用在**偏移**上、且是**逐帧阈值**
    //   ⇒ 一旦转开 10°，偏移就是 10°，叠在上面的手抖**原样通过**，门限根本不参与。
    //   ⇒ 门限只在"接近参照"那一段有用，**治不了转开之后**。故取**低通**。
    //
    // 【修法】对三个偏移加一阶低通（与 `FORCE_BIAS_EMA_TAU_S` / `FORCE_GUARD_EMA_TAU_S` 同一写法）：
    //   大而慢的**故意转动原样通过**（慢于 τ），手抖被压掉（快于 τ）。
    //   τ=0.25 s ⇒ fc ≈ 0.64 Hz ⇒ 对 5 Hz 的手抖衰减约 **8 倍**（1° → ~0.13°，刀尖 200mm 处约 0.45mm）。
    //   ⚠ 代价是 ~0.25 s 的跟随滞后 —— 现场若觉得"发钝"，把 τ 往下调（0.1~0.15）。
    //
    // ⚠ **α 必须用【实测 dt】**（`dt/(τ+dt)`）—— 本项目在 `ForceCompensation::step` 上正是栽在
    //   "按假定的采样率算 α"上（那次差 17 倍）。本函数的真实调用率是 **30 Hz**（ServoP 节流）。
    // ⚠ 间隔过大（> MAX_GAP）⇒ **重新对齐**而不是沿用旧滤值，否则会把一段陈旧姿态当增量补进去。
    //   参照那一组在 `onButton2Press` 里重设 ⇒ 本滤波器**必须在那里同时重置**（否则上一次按的残留会串进来）。
    const bool   ORIENT_STYLUS_LPF_ENABLED  = true;
    const double ORIENT_STYLUS_LPF_TAU_S    = 0.25;   // 一阶低通时间常数 (s)
    const double ORIENT_STYLUS_LPF_MAX_GAP_S = 0.5;   // 超过它就不滤波, 直接对齐 (s)

    const double SAFE_RX_MIN = -180.0, SAFE_RX_MAX = 180.0;  // Roll 安全限位
    const double SAFE_RY_MIN = -90.0,  SAFE_RY_MAX = 90.0;   // Pitch 安全限位
    const double SAFE_RZ_MIN = -180.0, SAFE_RZ_MAX = 180.0;  // Yaw 安全限位

    // ========== 虚拟约束力参数 ==========
    // ★★★ 虚拟约束力（触觉安全提示）总开关 —— 2026-09-21 由用户拍板【全部关闭】。
    //
    // 【关掉的是哪些】它们都在 haptic/HapticCallback.cpp 的第 8b/8c/8d 段, 叠加到推给 Touch
    //   的合力上（与 8a 的传感器力反馈相加）:
    //     8b SafetyPredictor::computeConstraintForce = 四个力之和
    //          ① 安全边界   (SAFE_X/Y/Z 内 50mm 起, 远离边界, 最大 2.0N)
    //          ② 圆柱奇异   (半径 < 80mm, 径向向外, 最大 2.5N)
    //          ③ 报警历史   (真报过警的位置 80mm 内, 远离它, 最大 1.5N)
    //          ④ 工作空间边缘 (半径 > 550mm, 向心, 最大 1.0N)
    //     8c Orient extra force        (奇异避免的约束放大)
    //     8d Orient directional repulsion (Phase 2 腕部对齐斥力, 仅按钮2)
    //
    // 【为什么关】书写演示里它们干扰手感。⚠【具体是哪一路在推, 现场没有测出来】——
    //   按实测的书写区域 (x,y 半径 ≈261mm、z 366~489mm) 只有 ① 够得到 (z 距 SAFE_Z_MAX=500
    //   只剩 11mm, 而感应距离 50mm); ② 要半径 <80mm、④ 要半径 >550mm, 都够不到。
    //   用户决定【全部关掉】。
    //
    // ★★ 【代价 —— 必须兑现, 不能只写在注释里】操作员从此【失去全部触觉安全提示】:
    //    靠近安全边界 / 奇异 / 工作空间边缘 / 历史报警点时, 手上【不会有任何推力】。
    //    ⇒ 补偿措施 (都已实现, 见 RelayCore::reportPosition 里那一段):
    //      · client 控制台: 启动横幅 + 每 60s 复报一次 (单靠注释不算"警告")
    //      · MATLAB 端: 走现成的 W| 通道每帧重发一条 level=2 的警告, 界面顶栏会被警告染色。
    //        ⚠ 必须【重发】: relay_gui.m 每个刷新周期 (0.05s/20Hz) 会把 warn_max_level 清零。
    // ⚠ 【不受影响】: 机械臂自己的碰撞检测与安全皮肤由机械臂侧做 (SetCollisionLevel/
    //   SetSafeSkin), 与本开关无关 ⇒ 真会撞的时候机械臂仍会停。
    // ⚠ 代码保留而不删: 翻回 true 即恢复; 且 test_constraint_force / test_singularity_avoidance
    //   测的是【计算】本身 (不是"有没有叠加"), 所以它们仍然覆盖这些力。
    const bool FORCE_CONSTRAINT_FORCES_ENABLED = false;

    const double CONSTRAINT_BOUNDARY_RANGE      = 50.0;   // 安全边界感应距离 (mm)
    const double CONSTRAINT_BOUNDARY_MAX_FORCE  = 2.0;    // 安全边界最大约束力 (N)
    const double CONSTRAINT_SINGULAR_RANGE      = 80.0;   // 圆柱奇异感应距离 (mm) — 提前预警
    const double CONSTRAINT_SINGULAR_MAX_FORCE  = 2.5;    // 圆柱奇异最大约束力 (N) — Touch 最大输出
    const double CONSTRAINT_ALARM_HISTORY_RANGE    = 80.0; // 报警历史感应距离 (mm)
    const double CONSTRAINT_ALARM_HISTORY_MAX_FORCE = 1.5; // 报警历史最大约束力 (N)
    const double CONSTRAINT_WORKSPACE_EDGE_START     = 550.0; // 工作空间边缘感应起点 (mm) — 提前预警
    const double CONSTRAINT_WORKSPACE_EDGE_MAX_FORCE = 1.0;   // 工作空间边缘最大约束力 (N)

    // ===== Singularity Avoidance (零空间优化) =====
    // Shoulder safety
    const double SINGAVOID_SHOULDER_SAFE_R     = 120.0;  // 肘部安全 r_xy (mm) — 低于此触发零空间优化 + 姿态模式 TCP 微调
    const double SINGAVOID_SHOULDER_CRITICAL_R  = 50.0;   // 肘部危险 r_xy (mm) — 姿态模式下触发临界警告
    // Elbow safety
    const double SINGAVOID_ELBOW_MID_ANGLE      = 0.0;    // J3 最佳位置 (deg) — 零空间吸引子中心
    // Joint limit repulsion
    const double SINGAVOID_JOINT_WARN_MARGIN    = 10.0;   // 关节限位警告裕度 (deg) — 零空间斥力触发距离
    // TCP micro-adjust
    const double SINGAVOID_MAX_POS_ADJUST       = 5.0;    // 姿态模式 TCP 微调上限 (mm)
    // Wrist damping (orientation mode) — Phase 2: continuous curve
    const double SINGAVOID_COND_WRIST_EARLY     = 20.0;   // 腕部早期预警阈值 — 开始触觉斥力
    const double SINGAVOID_COND_WRIST_BLOCK     = 150.0;  // 腕部阻断阈值 — β→0
    // Combined mode damping
    const double SINGAVOID_COND_FULL_WARN       = 15.0;   // 全控模式条件数警告阈值 (Phase 2: 30→15)
    const double SINGAVOID_SINGULAR_RATIO       = 0.05;   // σᵢ/σ₁ 阻尼触发比
    // Null-space optimization
    const int    SINGAVOID_NULLSPACE_ITER       = 20;     // 最大零空间迭代轮数 (Phase 2: 15→20)
    const double SINGAVOID_GRAD_STEP            = 0.3;    // 梯度步长 (deg) — Phase 2: 0.1→0.3
    // Gradient weights (should sum to ~12)
    const double SINGAVOID_W_SHOULDER           = 4.0;    // 肩关节安全权重 (Phase 2: 3.0→4.0)
    const double SINGAVOID_W_ELBOW              = 2.0;    // 肘关节弯曲权重
    const double SINGAVOID_W_JOINT              = 5.0;    // 关节限位权重
    const double SINGAVOID_W_WRIST              = 1.0;    // 腕部对称权重
    // Orient mode constraint force amplification
    const double SINGAVOID_ORIENT_FORCE_AMP     = 2.5;    // 姿态模式下奇异斥力放大倍数 (Phase 2: 1.5→2.5)
    // Directional repulsion (Phase 2 new)
    const double SINGAVOID_SINGULAR_FORCE_MAX_N = 3.0;    // 方向性斥力上限 (N)
    // Dual-singularity detection (Phase 2 new)
    const double SINGAVOID_DUAL_SING_COND_THR   = 60.0;   // 腕部条件数阈值 — 双重奇异检测
    const double SINGAVOID_DUAL_SING_ELBOW_THR  = 80.0;   // 肘部 r_xy 阈值 — 双重奇异检测

    // ========== 连接健康监控参数 ==========
    const int HEARTBEAT_TIMEOUT_MS  = 500;    // 心跳超时 (ms)
    const int RECONNECT_MAX_RETRIES = 5;      // 最大重连次数
    const int RECONNECT_BASE_DELAY_MS = 1000; // 重连基础延迟 (ms), 指数退避
    const int PING_INTERVAL_MS      = 500;    // PING 间隔 (ms)
    const int PING_TIMEOUT_MS       = 500;    // PING 超时 (ms)

    // ========== 错误升级参数 ==========
    const int ESCALATE_WARN_TO_DEGRADE   = 3;   // WARN 连续帧数 → DEGRADE
    const int ESCALATE_DEGRADE_TO_REJECT = 10;  // DEGRADE 连续帧数 → REJECT
    const int DEESCALATE_CLEAR_FRAMES    = 30;  // 清除后多少帧降级

    // ========== 看门狗参数 ==========
    const int WATCHDOG_TIMEOUT_MS = 200;     // 触觉线程看门狗超时 (ms)

    // ========== 升级时间阈值 ==========
    const int MIN_WARN_MS    = 50;           // WARN 至少持续 50ms 才能升级到 DEGRADE
    const int MIN_DEGRADE_MS = 200;          // DEGRADE 至少持续 200ms 才能升级到 REJECT

    // ========== 诊断日志参数 ==========
    constexpr const char* DIAGNOSTIC_LOG_PATH = "robot_diagnostics.log";
    constexpr const char* FORCE_LOG_PATH = "force_demo_log.csv"; // 力反馈演示落盘路径

    // ========== 发送队列参数 ==========
    const int MAX_QUEUE_SIZE = 5;            // 队列容量上限（满时丢弃旧数据）
    const int TCP_SEND_INTERVAL = 10;        // 发送间隔 (ms)
    const int FEEDBACK_TIMEOUT = 2000;       // 反馈读取超时 (ms)
    const int ALARM_CHECK_INTERVAL = 300;    // 报警巡检间隔 (ms)
    const int POSE_QUERY_INTERVAL = 100;    // 位姿查询间隔 (ms)，驱动 3D 模型更新
    const int IDLE_SLEEP_MS = 1;             // 发送线程空闲休眠 (ms)

    // ========== 3D 投影参数 ==========
    const float AXIS_LINE_WIDTH = 3.0f;
    const double BASE_CAM_X = 0.0, BASE_CAM_Y = 70.0, BASE_CAM_Z = 240.0;
    const double NEAR_CLIP = 1.0, FAR_CLIP = 800.0, FOV = 45.0;
    const float MIN_ZOOM = 0.3f, MAX_ZOOM = 5.0f, ZOOM_STEP = 1.5f;
    const float ROTATION_SPEED = 0.5f;

    // ========== 交互参数 ==========
    const int MAX_TRAIL = 300;

    // ========== Logo 参数 ==========
    constexpr const char* LOGO_PATH = "pics/NINELAB.png";
    const int LOGO_WIDTH = 150, LOGO_HEIGHT = 75;

    // ========== 颜色定义 ==========
    const float COLOR_FLOOR[4]       = { 0.22f, 0.25f, 0.30f, 0.55f };
    const float COLOR_BORDER[4]      = { 0.62f, 0.68f, 0.78f, 0.50f };
    const float COLOR_AXIS_X[4]      = { 1.00f, 0.35f, 0.35f, 0.95f };
    const float COLOR_AXIS_Y[4]      = { 0.35f, 0.95f, 0.45f, 0.95f };
    const float COLOR_AXIS_Z[4]      = { 0.35f, 0.55f, 1.00f, 0.95f };
    const float COLOR_CURSOR_DOT[4]  = { 1.00f, 1.00f, 1.00f, 0.95f };
    const float COLOR_TEXT[4]        = { 0.92f, 0.96f, 1.00f, 1.00f };
    const float COLOR_SUCCESS[4]     = { 0.35f, 0.90f, 0.50f, 1.00f };
    const float COLOR_ERROR[4]       = { 1.00f, 0.35f, 0.35f, 1.00f };
    const float COLOR_WARNING[4]     = { 1.00f, 0.78f, 0.28f, 1.00f };
    const float COLOR_TRAIL[4]       = { 0.25f, 0.85f, 1.00f, 0.90f };

    // ========== 坐标表格参数 ==========
    const int TABLE_LEFT = 15;
    const int TABLE_TOP = WINDOW_H - 100;
    const int TABLE_WIDTH = 230;
    const int TABLE_COL1_W = 70;
    const int TABLE_PADDING = 10;
    const int TABLE_TITLE_ROW_H = 34;
    const int TABLE_ROW_H = 26;
    const float TABLE_BG_COLOR[4]       = { 0.10f, 0.13f, 0.17f, 0.82f };
    const float TABLE_BORDER_COLOR[4]   = { 0.42f, 0.56f, 0.78f, 0.85f };
    const float TABLE_ALT_ROW_COLOR[4]  = { 0.12f, 0.16f, 0.21f, 0.82f };
    const float TABLE_CELL_TEXT_COLOR[4]= { 0.92f, 0.96f, 1.00f, 1.00f };
    const float TABLE_TITLE_BG[4]       = { 0.16f, 0.28f, 0.55f, 0.95f };
    const float TABLE_TITLE_TEXT_COLOR[4]={ 0.95f, 0.98f, 1.00f, 1.0f };
    const float TABLE_BORDER_WIDTH = 1.0f;

    // ========== 状态栏参数 ==========
    const int TCP_STATUS_BAR_HEIGHT = 30;
}
