# 上机清单 —— 2026-09-21 晚

> 目的：让软件侧**已经做完但从未在实机验证过**的东西，今晚拿到真数据。
> 本清单只列**需要机械臂**的事；离线能做的已单列在 §0。
> **★ 每一节的"带回来的东西"才是交付物。只带结论、不带原始输出，等于没做。**

---

## 0. 前置（离线，必须先完成）

- [x] **读取器已改成"对不认识的列数【响亮拒绝】"**，且用例在夹具带参考量列时**消费真列**。
      **⚠ 这一条是第 1 项的前提。** 不做它，第 1 项采回来的 31 列夹具会**正常加载、套件通过**，
      而参考量那一列**从未被读取** —— 收口会"看起来做完了、其实什么都没验证"。
      **✅ 已落地（2026-09-21）：** 读取器在 `2168be0`、用例消费真列在 `54475de`。开工前照旧核一眼即可。
- [ ] 完整构建通过：`cmd.exe //c "D:\Projects\Touch\Touch_Client\build.bat"` → `Build OK.`
- [ ] 构建前确认 **`Touch_Client.exe` 没在运行**（否则 `LNK1168`）。**不要杀它。**

---

## 1. ★ 重采带 `@720` 的夹具（今晚最重要的一项）

**为什么：** 现有四份夹具采于 2026-09-19，**表头没有 `F720*` 六列**（记录程序 09-20 才加）。
于是 `test_payload_calibration` 里那条"这些录像必须被**拒绝**"的断言**问了数据答不了的问题**，
被**刻意留红**。重采是为了让那条断言**给出真结论**。

**做法（与原采法完全相同）：**
- 笔尖悬空、**只改姿态**、**不动负载**；7~10 个姿态；
- 让记录程序写满末尾那六列 `F720x…M720z`（**31 列**）。

**⚠ 三个陷阱：**
1. **采完立刻核表头有没有 `F720*` 六列**（`grep -m1 F720 <文件>`）。**没有就白采了。**
2. 读取器是否已改成响亮拒绝（§0 第 1 条）。否则它会把 31 列截到 25 列、悄悄丢掉那六列。
3. **不要改负载** —— 这一项只改姿态。改负载会让结论失去意义。

**采完之后：**
- 文件放进 `Touch_Client/tests/fixtures/`，命名与既有风格一致（如 `calib_poses_2026-09-21.txt`）；
- 跑 `test_payload_calibration`。

**期望：** 那条断言**不再"必然红"**，而是给出一个**真结论** ——
**"放行"或"拒绝"都是有信息的答案**（这些采集真值外力 ≈0，所以"放行"很可能是对的；
但**没有数据之前不许断言**，见计划里那句"正确答案很可能是放行"）。

> **⚠ 2026-09-21 晚 · 按当前代码核对后的更正（写清单时还没核过这一条）:**
> **只把新文件放进 `tests/fixtures/` 并重跑，上面那个"期望"不会自动成立。**
> 那条留红的断言（`runtime_consistency_guard_replay`）**在用例里硬写了四份 2026-09-19 的文件名**，
> 测试目录**不做任何目录扫描** ⇒ 新采的 `calib_poses_2026-09-21.txt` **不会被它读到**，
> 跑完仍是 **`71 passed / 1 failed`**。
> ⇒ **要在今晚让它给出真结论，必须同时把那个文件名列表指到新采集上**（或替换掉其中一份）。
> 这一步是**改测试代码**，不在本清单的"离线前置"里 —— **今晚开工前先定：是谁、何时改这一行。**
> **另一条路已经通了：** `54475de` 已用一个**指向已入库快照的新用例**拿到了真结论
> （见上面"★ 提前知道"一节），所以**即使这一项不改，今晚也不是空手。**
> **⇒ 别把"新夹具加进去了、计数还是 1 红"读成"采集失败"。**

**带回来的东西：**
- [ ] 带 `F720*` 六列的夹具文件（**表头已核过**）
- [ ] `test_payload_calibration` 跑完的**完整输出**（含那条断言的结论与它打印的逐姿态结果）

### ★ 提前知道：`@720` 的原始数据**其实已经在检出里**（2026-09-21 晚补记）

`Touch_Client/tests/fixtures/calib_poses_2026-09-20_frozen.txt` 里**已经有三段带 `F720x…M720z` 的采集**
（`21:58:46` / `22:12:28` / `22:18:42`，各 10 行 = 5 对原地复采；文件混有 18/25/31 列）。
**但全仓库没有任何读取器加载它** —— 这就是那条断言至今仍是"问了数据答不了的问题"的**真正原因**，
而不是"数据不存在"。

**⇒ 于是今晚有了第二条路：把回放用例指向这份快照，不上机也能让那条断言给出真结论。**

**⚠ 但要打一个方法论折扣（很重要）:** 容差的第二项 (乙) **就是从这三轮推出来的**
⇒ 拿同一份数据去验证闸门是**部分循环论证** —— 正是本项目记过的那条账
（"验证手段与实现共享假设时，全绿是零信息量"）。
**⇒ 它能给出"真结论"，但强度【弱于】新采的独立数据。**

**⇒ 建议两条都做：** 离线先拿一个真结论（今晚之前就能有），上机再拿**独立**确认。
**且上机那趟仍然值得去** —— 下面 §2~§7 那几项（符号约定 / `@1037` / 漂移检查 / `Fz` / `A` 第三行）
与这份夹具**无关**，只能上机做。

### ⚠ 另一条坑：若新的 `@720` 与本地模型差超容差

那闸门会拒、`compensated` 被置零，而**另一个**用例
（`test_runtime_compensation_pose_independence`）会以
`口径自校不过 - 量错了东西` 的样子红 —— **那句话会把诊断方向带偏**。
**遇到它先问一句：真正的原因是不是闸门在拒？** 而不是去查量纲。
（参考量：实测 `|comp_z − @720_z| ≤ 0.25 N`，而容差 `1.2464 N` ⇒ 正常应当放行。）

---

## 2. 复读 `@1037`（`sixForceOnline`）

**为什么：** 新的"参考量可用性"判据用 `sixForceOnline == 1`，
而它的依据**只是夹具文件头里的历史记录**，**不是今天测的**。
若健康的臂报出非 1，闸门会**永久拒绝**（fail-closed，消息会指名这个字段）——
那时要改的是**判据**，不是把它当成故障排除。

**做法：** 客户端跑起来、臂在线、力帧在流时，看闸门那行打印里的 `@1037 = ?`

**带回来的东西：**
- [ ] `@1037` 的**实测值**（必须是 `1`）

---

## 3. 观察闸门是否真的放行（`@720` 那一路）

**为什么：** 这是"传感器力那一条路"第一次有可能真的通。软件侧已确认：
一处参考量、一处掩码、一次比较、全或无置零在每条拒绝路径上都保持；**但没有在实机上验过**。

**做法：** 启动客户端 → 看闸门状态。若为 OK ⇒ **力数据第一次真的会往下走**。

**带回来的东西：**
- [ ] 闸门状态（OK / 拒绝）+ 如果拒绝，**逐通道表里是哪一路超限**

---

## 4. 零偏漂移检查（它首次真的会跑）

**★ 先记住这条，否则会误判：** 它比闸门**更紧** ——
它 `FORCE_ZERO_DRIFT_WARN_N = 0.5 N` 就报警，而闸门到 `1.2464 N` 才拒。
⇒ **闸门放行后它可能立刻报漂移 —— 那是它在起作用，不是故障。**

**做法：** 启动后等它定稿，看它报什么（它会说"正常"或给出 `drift` 值）。

**带回来的东西：**
- [ ] 它报的 `drift` 值，以及它给出的两条建议（按 `'z'` 重调零 / 按 `'m'`+`'s'` 重标负载）哪条符合现场

---

## 5. ★ `@720` 的符号约定（今晚唯一能验它的机会）

**为什么：** 全库**没有任何文件**给出 `@720` 的符号约定。
静止时 `comp≈0` 且 `@720≈0` ⇒ **"两者一致"是零信息量** ⇒
**换参考量这件事，至今只在静止态被验证过。**

**做法（要小心）：** 在**安全范围内**施加一个**方向已知**的小外力
（用手轻推笔尖、或挂一个已知小质量），观察：
**`@720` 的变化方向**与**我们算出的 `comp` 的变化方向是否同号**。

**★★ 若符号相反：立刻停下来报告。** 那不是调参问题，是设计问题 ——
真实外力下闸门会看到约 **2×** 的偏差而非接近 0。

**安全红线：** 施加的力要在安全边界内、**不要触发碰撞检测**、**不要动负载**。
**有疑问就不做这一项，标记为"未做"** —— 不做的代价只是"仍未验证"，做错的代价是别的。

**带回来的东西：**
- [ ] 施加外力的方式与大致量级
- [ ] `@720` 与 `comp` 在**那一次**的变化方向（同号 / 反号）
- [ ] 或明确写"本项未做"

---

## 6. `Fz` 该不该投票（**依赖第 5 项的数据**）

**现状：** 它不投票的依据（老参考量的 z 响应秩 2）**已复测、不复现**。
**⚠ 机制要说准：** z 行的**绝对量几乎没变**，塌的是 **x/y** ⇒ "z 不再被结构性压小"说的是**比值**，
**不是 z 被修好了**。

**★ 在第 5 项的数据到手之前，不要改掩码。** 那条参考量在【接触状态】下未经验证。

**带回来的东西：**
- [ ] 若第 5 项有结论，记下它支持/反对 `Fz` 投票的方向（**今晚不改掩码**）

---

## 7. `A` 第三行的绝对界（能做就顺手做，不能就跳过）

**为什么：** 它**没有绝对界** —— 无 z 真值、机械臂模型与我们不独立 ⇒ **共模误差不可见**。
现在 `Fz` 不投票（且力矩本次也退出了投票），所以 ~2 N 的第三行误差会**无守门通过**。

**做法：** 吊一个**已知质量**（静态、笔尖不接触任何东西），把 `comp` 的 z 分量与期望值比。

**带回来的东西：**
- [ ] 砝码质量 + `comp` 的 z 分量读数（或写"无合适砝码，未做"）

---

## 今晚必须带回来的东西（汇总）

- [ ] 带 `F720*` 六列的夹具文件，**表头已核**
- [ ] `test_payload_calibration` 的完整输出（那条断言给出**真结论**了）
- [ ] `@1037` 实测值
- [ ] 闸门是否 OK；若拒绝，是哪一路
- [ ] 漂移检查报的数
- [ ] 外力方向实验：同号 / 反号 / 未做
- [ ] `A` 第三行：砝码读数 / 未做
- [ ] **任何异常现象的原始输出**（不要只带结论回来）

---

## 安全红线（照旧，逐条）

- **不要杀运行中的 `Touch_Client.exe`**
- **不动负载**（第 1/3/4/5/7 项都不需要改负载）
- **1.5 kg 太重** —— 昨天实测会让机械臂迅速运动报错，已记过账
- 外力实验只在**安全边界内**、**不触发碰撞检测**；有疑问就不做
- 任何一项拿不准，**标记"未做"并说明** —— 本项目里"没查必须有句话"

---

## 附：今晚**不要**做的事

- **不要**为了让套件好看而改那条刻意留红的断言
- **不要**在拿到第 5 项数据前改 `Fz` 的掩码
- **不要**把"有单测覆盖"说成"已在实机验证"—— 至今没有一条实机验证
- **不要**说套件"全绿"（截至 `54475de`（2026-09-21）：`test_force_compensation` `39/0`、
  `test_payload_calibration` **`71/1`**，那 1 条是刻意留红）
  - ⚠ **这是当时值，不是常数。** 用例会继续增减 ⇒ 引用前重跑一次，别照抄这一行。
  - ⚠ **旧值提醒：** 本清单与 `2026-09-21-remaining-work.md` 早先写的是 `69/1`；那是
    `54475de` 之前的数，**已过期**（差 2 条：`2168be0` 与 `54475de` 各加了一条用例）。
