# 2026-09-23 上机记录：C2 全过 · D2 换口径 · 三个静默缺陷 · 按钮2 受阻于钳位

> 机器：CR3 + Touch。客户端 `x64/Release/Touch_Client.exe`（18:24 重建，0 error）。
> **取证方式（当晚新加的）**：客户端 stdout 重定向到 `Touch_Client/session_2026-09-23*.log`；
> MATLAB 侧由 `relay_gui.m` 里的旁路日志 `tlog()` 落到 `Relay_Station/_matlab_session_20260923.log`。
> ⇒ 两侧的协议流量落在文件里，事后可逐条对齐（不再依赖口头转述）。
> ⚠ 当晚**产品本身（exe / relay_gui）与取证用的重定向是两件事**：控制台窗口是空白的、属正常。

---

## 一、C2（阻力映射系数可调）：四项全部实测通过

| 项 | 判据 | 实测 |
|---|---|---|
| 1 `RG\|` 回读走通 | 滑条解锁 + 显示 C++ 的 range | ✅ **三次独立复现**（客户端启动 / MATLAB 重载 / 客户端重启）。线上原文 `RG\|120,100,300,1.9800,0.2,1.66667,120`，七字段与源码逐一对上；`SET value=120.000` 与屏幕一致 |
| 2 拖动生效 | 手上力跟着变 + 落盘 | ✅ 104 条 `RG\|` **全部被接受**（拒 0）；**`Touch_Client/calib/force_tuning.json` 18:51:46 首次产生**（`tick()` 开头 `if (!s_dirty) return;` ⇒ 出现即证明改值被接受）；中间值 `140→155.6→192.2→226.4→134.4→130.4→125→120` 均有落盘行 |
| 3 `[Zero]` | 真调零 + `C\|` 回显 | ✅ `UI Zero button pressed` → 12 ms 后 `OUT Z\|1` → **`IN C\|[Force] 调零中…(来自 MATLAB)`** 到面板 → 活标定 `force_calib.json` 18:55:23 被重写 → 用户实测力被调零 |
| 4 真鼠标拖动 | 真实事件 | ✅ 与 2 同一次（104 次回调、相邻中位 44 ms） |
| ★ 100ms 限频真实时序 | 与设计一致 | ✅ 拖动是**爆发式**的（103 个间隔里 75 个 <100 ms）⇒ 按 100 ms 分桶只有 58 个"有改动的桶"，实际回读 44 ⇒ 合并是设计行为。⚠ **判读陷阱**：MATLAB **每 50 ms 排空一次 socket**，时间戳被量化到 50 ms 网格 ⇒ 会出现 46/88/93 ms 的**假**"间隔"，不能据此说限频被突破（真突破的特征是**同一批 ~0–1 ms 成对**，实测没有） |
| ★ 重连竞争（此前未测） | MATLAB 重启后自动恢复 | ✅ 两轮**从零握手**（`wasKnown=0`）⇒ **MATLAB 重启不需要重启客户端** |

**A3 顺手取到**：`零偏漂移检查: 补偿后读数 0.14573 N / 0.114717 N`（阈值 0.5）—— 比 09-22 的 0.4025 N 健康得多。

---

## 二、D2 运动噪声重测：**判据口径本身是错的**（已订正）

**★ 追出处才发现**：执行单原判据「运动噪声从 0.17 N 落到 ~0.02 N（静止侧已实测 0.0224 N）」
把两个**不同量纲**的数摆在一起比：
- `0.0224 N` **不是静止噪声** —— 它是 `test_payload_calibration` 的
  `test_runtime_compensation_pose_independence` 量出的**姿态间散布**（离线夹具，见执行单 :182 表）；
- `0.17 N` 是**运动时**的时序抖动。
- ⇒ 这条错位**已经抄进项目记忆**（"静止侧已实测 0.0224 N"），当晚追出处才揪出。

**`'n'` 探针答不了这个问题**：它量的是**原始 @1304、未补偿**（与 0.17/0.0224 不是同一仪器）。
探针本身没错 —— 它答的是"过采样有没有收益"：**帧率 122.8 / 122.7 Hz、最大间隔 10.5 / 10.2 ms
（文档 125 Hz 对得上）**，静止段判出 **✔ 宽带噪声、跟着 1/√k 掉** ⇒ 过采样有收益。

**口径正确的量（落盘口 `filtered`，`RelayCore.cpp:2482` 传的就是它）**：

| 工况 | n | **Fz sd** |
|---|---|---|
| **写字式轻慢运动**（取段内平稳的前半段） | 548 | **0.0279 N** |
| 空闲静止 | 556 | 0.0313 N |
| 大行程运动（Z 432→210 mm） | 1414 | 0.4929 N（惯性项，**不是**噪声底） |
| 旧定标依据 | — | 0.17 N（工况不明） |

- ⚠ **整段 sd 0.0921 N 不作数**：前半均值 −0.0037 / 后半 −0.0780，半段差远超 3σ ⇒ 后半段是
  真实载荷变化（笔压/接触 = 信号）。**这套自检与探针的平稳性体检同一规矩。**
- ⇒ **写字工况残余噪声 ≈0.028 N，与空闲底 0.031 N 同水平** ⇒ 轻慢运动几乎不加噪声；
  **死区 0.20 N 是它的 ~7 倍**（旧账按 0.17 N 定，裕量只有 1.2 倍）⇒ **噪声不再是最紧的约束** ——
  这是 ④ 需要的输入。**用户缓办，本趟不动。**
- ⚠ 口径边界：sd 是**落盘口 11 Hz 采样**后的；`92 ms / 10.9 Hz 的落盘节拍是设计**
  （`pollForce()` 注释：09-21 起补偿+滤波已搬到 ForceReader 线程按帧率跑）——**不是回归**。

---

## 三、当晚查出的三个**静默**缺陷

### 3.1 `RP|` 解析下标错**一个字符** ⇒ 面板两行恒 0（**已修 + 已验证**）

`relay_gui.m`：`startsWith(msg,'RP|')` 之后写的是 `msg(3:end)`，而 `'RP|'` 是**三个**字符
⇒ 载荷从 `|` 起、`sscanf('%f,…')` 撞非数字**立刻返回空** ⇒ `length(vals)==6` 恒假
⇒ **`S.robot_pos` 一次都没被赋过值**。

**代价两处，都静默**：面板 `Position` / `Orientation` 两行恒 0（现场报告"orientation 恒为 0"）·
3D 里 `eeMarkerActual`（实际位置球标）被 `if any(rp(1:3)~=0)` 挡住 ⇒ **从未显示过**。

**证据**：headless 实测 `sscanf('RP|12.34,…'(3:end))` 长度 **0**、`(4:end)` 长度 **6**。
同文件 `RG|`（也是三字符）早踩过并写下警告，**本支漏修**；`FB|`（三字符）是对的。
**C++ 侧已排除**：`robotActualPose` 唯一周期性写入者是 `queryPose()`、`tcpPoseActual` 从不拷进它，
而日志里 `pos=`(GetPose) 与 `tcp=`(30004 帧) **30/30 逐位相同**且跨运行变化 ⇒ `RP|` 确实发出去了。

### 3.2 `sendRelayUpdate` 帧界 bug ⇒ **静默丢消息**（**未修**）

它**分两次 `send()`** 发（消息 + `"\n"`）、**无锁**、而**两个线程**都在调
（触觉线程发 `P|`、主线程发 `RP|`/`J|`）⇒ 交错时**换行丢失、下一条被粘上**：

```
L|274.9,311.9,68.6,331.2,275.8,300.1P|0.00,-65.51,-88.11,-23.50,6.60,14.92
                                    ↑ 第二条对 MATLAB 而言【不存在】
```

**实测：109568 行里 54 行如此**（≈0.05%）。修法：整行（含换行）**一次 `send`** + 跨线程加锁 /
循环写完（`send` 的返回值也不该只判 SOCKET_ERROR —— 短写同样会截断）。

### 3.3 我自己加的旁路日志把 MATLAB 界面卡死（**已修**）

现场报告"按住按钮时界面卡死、松手才恢复"。机制：`tlog` 每条一次 `fopen/fprintf/fclose`，而它跑在
**收包回调**里（正是服务界面的那条线程）；实测 **~39 条/秒 ⇒ 上百次文件 I/O/秒** ⇒ 界面被饿死；
按钮一松、`C|ServoP` 停了、消息率骤降就"活了"。
**⚠ 这是"测量工具扰动了被测对象"** —— 改法是降开销，不是少测点：
持句柄写 + **每秒 `close`/`reopen` 一次**（⚠ 实测 `fseek(fid,0,'cof')` **不刷新** —— MATLAB 还在跑时
另一进程读到空文件，只持句柄会让实时读取失效）+ **同内容去重**（`W|` 是设计上 30 Hz 重发、21 条/秒
且文本逐拍相同 ⇒ headless 实测 300 条输入→124 行，信息不丢）。
顺手把 `P|`/`J|`/`RP|` 从"一律不记"改成**抽样记**（每 10 条 1 条）—— 全滤掉会让日志**回答不了**
"这几路到底有没有到"，而 3.1 正是卡在这里。

**验证**：`checkcode` **19 → 19**，逐条同分布（过程中引入的 `now`、"未使用持久变量"两条 lint 被它抓出并修掉）；
现场确认重载后卡死消失。

---

## 四、按钮2 姿态映射：**本趟测不出来，根因是钳位饱和**

### 4.1 为什么测不出来（数据）

在用户完成四组动作（纯平移 / 点头 / 摇头 / 捻笔）后：

| 量 | 实测 |
|---|---|
| `\|rx\| > 170` 的样本占比 | **85.8%**（550/641） |
| rx 取值分布 | **+180 出现 208 次、−180 出现 117 次**（325 次精确落 ±180）⇒ **一直在接缝上翻转** |
| 最小二乘系数 | 全部 ≤ **0.07**（角与位置两块都是）⇒ 环**饱和**，无信息 |

⇒ **关于 rx 的部分全部不能用**；关于 ry/rz 的部分（用户的两条定性观察）**不受此钳位影响，仍有价值**。

**机制**：用户挪到的位姿是 `rx = −147.82`（**离接缝只有 32° 余量**），而姿态摆幅 ±50~80°
⇒ 几秒内撞到 ±180 被 `clampOrientToBounds` 夹住并**钉死**（那 325 次精确 ±180 + 反复翻转就是指纹）。

**更麻烦的**：这台臂做这个活时末端姿态**天然就在 rx ≈ ±180 附近**（笔朝下）⇒ **可能是
"操作员在 ±180 附近转不过去"的根源**。⇒ **要测出映射，先得处理这个钳位**（`SAFE_R*` 没有出处、
09-22 "环绕/规范化"那条修法现场撞出关节超速并已回滚留档）。

### 4.2 用户的定性观察（**照原样记，附适用边界**）

1. **纯平移 ⇒ 机械臂动了**（设计说应当不动）—— ⚠ **要打问号**：环饱和时无法区分"位置真的漏进姿态环"
   与"饱和产生的假象"，需在不饱和的环里重测；
2. **② 点头 与 ③ 摇头 的实际运动方式应当对调**；
3. **④ 捻笔 ⇒ 机械臂沿 y 轴运动，且没有自转现象**。

### 4.3 ★ 一条**不需要机器人**的测量捷径（下次先用它）

**笔杆的姿态角 `P|` 的后三位与机械臂无关** ⇒ 只要读 `P|`，让操作员把笔按**左/右摆、前/后摆、自身
拧**三种方式各动一次，就能定出**笔杆三轴 ↔ 物理动作**的对应与符号 —— **不受钳位影响、不占机器**。
（本趟把这条留在了后面，是因为先按执行单去量了机械臂；应当**先定笔杆系**。）

---

## 五、★ 按钮2 的新规格（用户口述，2026-09-23 —— 作为验收标准）

> 1. **笔杆平移时机械臂不动**；
> 2. **笔尖左（右）摆动** ⇒ 机械臂向 **X 轴负（正）** 方向摆动；
> 3. **笔尖前（后）摆动** ⇒ 机械臂向 **Y 轴负（正）** 方向摆动；
> 4. **笔杆转动时 J6 转动**（笔杆绕自身轴的拧转 ⇒ 末端 roll ⇒ J6）。

与现状的差距：当前实现是"**Euler 角作差 × 硬编码 3×3 置换**"再加到"按下时的机器人姿态参照"上，
而那张置换表 `robot_X=touch_X / robot_Y=−touch_Z / robot_Z=touch_Y` 在源码里**明写"从未被验证过"**。

---

## 六、状态与产物

- **分支** `fix/test-harness-orphan-suites`（名字比内容窄），本晚新增提交见 `git log`；**未推送**。
- **未提交**：无（本文件提交后工作区应干净）。
- **当晚产生的未入库产物**：`Touch_Client/session_2026-09-23*.log`（3 份，含 run1/run2 存档）·
  `Touch_Client/x64/Release/force_demo_log.csv` · `Relay_Station/_matlab_session_20260923.log` ·
  `Touch_Client/_run_captured.bat` 与 `_tail_log.bat`（我写的启动器/只读 tail 窗口）·
  `Touch_Client/calib/force_tuning.json`（**首次产生**）+ `force_calib.json`（被 `[Zero]` 重写）。

## 七、下一步（按用户指示）

1. ~~归档~~（本文件）；
2. **按第五节的新规格重做按钮2 的映射**；
   ⚠ 动手前先做 4.3 的**笔杆系测量**（不需要机器人）—— 否则"笔尖左右摆"对应哪个笔杆 Euler 角
   只能靠猜，而这条链上"猜错"的历史很长。
3. 未结：`sendRelayUpdate` 帧界 bug（3.2）· 三次 `mode=9` 报警与「逆解算无解」的出处 ·
   `SAFE_R*` 无出处 / ±180 钳位（**它同时挡着操作员的手感和 B2 的测量**）。
