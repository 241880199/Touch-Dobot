# 剩余待办 —— 2026-09-21（闸门计划执行完毕之后）

**上游：** 计划 `Docs/superpowers/plans/2026-09-21-gate-reference-and-moment-vote.md`（8 Task，已执行）
**今晚要做的：** `Docs/superpowers/specs/2026-09-21-on-machine-checklist.md`
**本文件只收"计划收口表之外的、离线能做的"与"只有用户能定的"。**

> ## ★★ 2026-09-21 晚：已上机（run-005）—— 本文件下面那张 D 表**已执行过一轮**，请先读记录
>
> **完整记录：** `Docs/superpowers/specs/2026-09-21-raw-channel-calibration-run-005.md`
>
> **D 表的结果（下面那张表本身不再重复，只报结论）：**
> | 项 | 本趟结果 | 该条状态 |
> |---|---|---|
> | §1 新采集 | **放行 10/10**（力通道余量 **Fx 3.53× / Fy 2.38×**）| **独立验证达成** ⇒ **关闭** |
> | §2 `@1037` | 块头实测 **1** | **关闭** |
> | §3 闸门实机 | **放行** | 首次真的通 |
> | §4 漂移检查 | **0.4025 N，正常**（阈值 `0.5` 的 **80.5%**）| 记一笔余量 |
> | §5 符号约定 | **同号**（两笔：55.1 N / 15.9 N 轴向）| **关闭** |
> | §7 砝码 | **未做** | **保留**（改由"吊一瓶水"做，见下） |
>
> **⚠⚠ 但同一趟冒出两条更重的问题（优先级高于本文件所有条目）。二者【互相掩盖】：**
>
> **① 在线零偏会【静默擦除】持续外力** —— `bF` 的 EMA 目标正是把 `comp` 拉向 0，
>   而它分不清"传感器零点慢漂"（该吸收）与"一个持续的外力"（绝不该吸收）。
>   **实测**：挂 1.9 N 重物、同一姿态 11.4 分钟，`force_demo_log.csv` 里 `|F|` 全程 ≈ 0.1 N
>   （而传感器上那 1.9 N 一直在）。
>   **⇒ 低于约 2.5 N 的持续工具载荷被擦除，闸门一直放行，没有任何东西报警。**
>   **写字力度（0.5~2 N）正落在这一档。** 见 run-005 §7.4。
>   **⚠ 我先前写的"任何 >1.25 N 的笔尖力会让闸门拒"【已被数据推翻一半】** ——
>   漏的就是这一步。正确的门是 `g_max = L/2 > 1.2464 ⇒ L > ~2.49 N`。**以 run-005 §7.4(d) 为准。**
>
> **② `@720` 在真实工具端载荷下与传感器差约 140 倍**（§7 + §7.4(b)；
>   而它【对传感器上游的扰动】却响应正常 —— 按小臂 3.8 N / 13 N·m 有响应）。
>   ⇒ **闸门的参考量在真实载荷下不可用。**
>
> **★★ 两者的关系**：`comp` 被吸收到 0、`@720` 本就 ≈ 0 ⇒ 闸门看到"两边一致" ⇒ **都藏起来。**
>   **⇒ 即使把 `@720` 修好，① 仍会让持续力消失。① 是更根本的那一条。**
>
> **本趟已加的仪器**：`'m'` 模式 `SPACE` 打印新增 `comp`（带闸门状态）+ 在线零偏 `bF`/`bM`。
> **下一步（30 秒，挂任何东西，不需要知道多重）**：连按几次 `SPACE`，看 `bF` 是否朝载荷爬
> —— 那会直接证实机制并量出时间常数。
>
> **本文件 A 节新增（由 run-005 §15 迁入，避免只留在记录里）：**
> - **A15** `多姿态零偏检查` 的判据读的是 `@576`（对负载不响应）⇒ 它那个 `✗ FAIL` **不构成证据**；
>   **但它不是一行替换**（`@720` 的力矩极差差 3.4 倍 ⇒ `drEq` 79→269 mm）。run-005 §9
> - **A16** 帧解析**没有魔数校验**（文档给的 `TestValue @48 = 0x0123456789ABCDEF` 全库零使用）；
>   唯一的 `sane` 检查只护 `@1168/@1176` 四个字段且**不触发重连**，而**力数据写在它外面**。
>   **当前偏移有闭环证据是对的**（回读 `@1168=0.404`、`@1176=(0.3,−0.1,68.7)`），但缺护栏。run-005 §12
> - **A17** **帧陈旧时 `raw`/`@720`/`@1304` 保持【上一帧的旧值】**（只有 `compensated`/`filtered`/`hapticOut` 被置零）
>   ⇒ 隐式行为，应写进注释。守卫是有的（采样作废 / 闸门可用性含 `!fd.isStale`）。run-005 §12.1
>
> - **A18** **`.bat` 文件必须 ASCII-only**（2026-09-21 实测踩到）：中文 `rem` 注释在非 UTF-8
>   代码页下被 cmd.exe 解码错，**静默吞掉下一行** —— 本次症状是 `vcvarsall.bat` 的 `call`
>   不生效、`'cl' is not recognized`、`BUILD_EXIT=9009`。本项目所有 `.bat` 的注释本来就是英文，
>   那就是这个原因。**新建 `.bat` 时照此办。**（已写进 `tests/build_frame_layout_test.bat` 内）
>
> **★ 两条曾口头报告、事后撤回的主张（勿采信）：** ~~`@720` 力通道不稳定~~ · ~~容差 (乙) 项偏小~~（run-005 §8）

> **⚠ 为什么要有这份文件：** 这些项此前只写在 `.superpowers/sdd/`（**被 gitignore，不入库**）
> ⇒ 一次 `git clean` 就没了。本项目的规矩是"**没落盘的等于不存在**"。
> 收口项（要上机的那些）在**计划自己的收口表**里，不在这里重复。

---

## A. 离线可做（不需要机械臂）

| # | 事项 | 位置 / 依据 | 价值 |
|---|---|---|---|
| A1 | **让挂钟可注入，别让那个测试真睡 5.2 秒** | 最终修复波新增的跃迁打印用例靠真睡一个复报周期（`FORCE_GUARD_REPORT_MS` + 200 ms）⇒ 慢、且是本项目一贯反对的"靠时间" | 中；顺带消掉一个不确定源 |
| A2 | **`Config.h` 引的推导文件里 `0.50 N` 仍读作现值** | `.superpowers/sdd/runtime-guard-report.md` §3 与逐通道表；`Config.h` 把**整份文件**引作 (甲) 推导来源 ⇒ 落在 §3 的读者看不到那行日期注 | 高（同"引了但出处里是旧值"这一类） |
| A3 | **三处硬引用的 `HapticCallback.cpp:168`** | `ForceCompensation.h` · `RelayCore.cpp` · 已修的那处是第三处；本项目反复吃过"注释里的硬引用会烂"的账 | 中 |
| A4 | **`runZeroDriftCheck` 是否还有静默分支** | Task 5 的委派就是"消灭静默路径"；上一轮补了两条、删了一条不可达的，**是否还有**需要按代码核一遍 | 中 |
| A5 | **z-gap 证据文档两处口径** | `Docs/superpowers/evidence/2026-09-21-z-gap-report.md` §7 仍印着借用的 `se 0.0010~0.0027 kg`（§5.2 已改）；§4.1 论证用的尺子偏松、方向恰利于结论 | 中（该文档的结论是 `Fz` 决策的依据） |
| A6 | **`ZERO_CHECK_MIN_SAMPLES` 无断言钉住** | 用例里硬编码同一个 10 ⇒ 漂了不响 | 低 |
| A7 | **"跑测试会往工作目录写文件"** | 两个套件按 **CWD** 写标定状态（实测：仓库根 `calib/force_calib.json` 被测试运行改写）⇒ 让它们写到临时目录，或至少在脚本里写明 | 中（否则每次跑都脏一次工作区，最终复审已因此误判过一次） |
| A8 | **`g_guardTol` 是容差的第二份映射** | 静态初值 + `init()` 重填；两份今天一致、且静态初值是为了 `init()` 之前 fail-closed ⇒ **复审判定可接受**。若要收成一处，注意别丢掉那个 fail-closed 性质 | 低（有意保留） |
| A9 | **回放用例里一句注释不精确（机制已知 —— 且量级已随 2026-09-21 的 α 改动缩了 180 倍）** | 测试说"8 帧同值 ⇒ EMA 恒等于该姿态的差"，实际**生产链路的在线零偏 EMA 在每个放行帧后生效**（回放时姿态不变 ⇒ `isStill()` 恒真），所以 8 帧 EMA 比该姿态的单次差偏低 —— 这**正是**同一文件里那句"来源未查明"。<br>**旧数**：α=0.01 ⇒ 每帧 ×0.99 ⇒ 8 帧低约 **0.0009 N**。<br>**现数**：α 由 `FORCE_BIAS_EMA_TAU_S = 600 s` 反算 ⇒ 约 `5.5e-5` ⇒ 8 帧 ×0.99956 ⇒ 低约 **9e-5 N（可忽略）**。<br>⇒ 那句注释仍应改成"已查明"，**并写上当前 α 是怎么来的**（否则下一个人会拿旧数去对） | 低，但顺手就可以确证 |
| A10 | **模型②"样本内 ⇒ 偏乐观"的理由是错的** | 对照目标是**实测的 `@720`**、不是 0 ⇒ 样本内拟合**不意味着**"更接近参考量"。复审自己的数字显示**第 1 轮模型②反而更差**（0.2088× vs 模型①的 0.1417×）。**姿态对（以已装标定为主）、理由错** ⇒ 改理由，别改姿态 | 低（注释准确性问题） |
| A11 | **排序理由那条也不准确** | 注释说"反过来的话旧用例的 `return` 会让新用例变成死代码" —— 不成立：它们是 `main()` 里**两个分开调用的函数**，且 `main()` 在 `g_failed++` 之后继续。**死代码问题在【同一个函数内部】**（那条断言一 return，它自己后面的 (乙) 反面对照永不执行）。排序本身仍对（有意义的结论先打印、红的噪声在后），**只是理由要换** | 低（同一类：注释与代码不符） |
| A12 | **单个坏掉的【投票】通道仍然只是警告** | 六列**全**为 0 / 非有限 / 离线 ⇒ 硬失败（`7beece8`）；但 **1~2 个通道坏、其余有数据 ⇒ 只警告**。而若坏的是 **`Fx` 或 `Fy`**（投票通道），那一通道的余量是 `comp − 0` —— **同一类"空比较"，只是窄化到一列**。实际风险低（六列是一次缓冲写入，部分为 0 需要逐通道 bug；"记录程序没写"那种会变成 25 列 ⇒ 直接 Broken ⇒ 失败） | 低（已明文记为有意为之） |
| A13 | **新采集上闸门若"拒绝"，套件【不会】变红** | 那一行是**只印不判**（`r.notCompared > 0 || r.poses != cap.n` 才是失败条件），故意的：让失败计数在任何今晚结果下都稳定在 1。代价：**拒绝信号只存在于 stdout** —— 只看退出码或 `74/1` 的人看不到。⇒ 已在清单里写明"看 `闸门判决` 那一行"。**若要让它变红，得接受失败计数会随数据变** | 中（今晚靠清单兜住；长期值得改） |
| A14 | **自检的合成行会混进操作者输出** | 两个自检用例对合成文件名调 `t6TakeFreshCapture`，于是输出里会出现 `未找到 _synth_…` 之类的行。已在行内标注是合成的，但**按 `未找到` 或 `★★` grep 的人会看见幻影** | 低（今晚认 `calib_poses_2026-09-21.txt` 那几行） |

**另有若干纯排版/风格项**（报告里编号重复、未加 `std::` 限定、用例对称性小缺口等），
目前**只**记在 `.superpowers/sdd/final-minors-triage.md` 的 A 节（**不入库**）。
若要长期跟踪，应把那一节搬进本文件；**否则它随 `git clean` 消失**。

---

## B. 只有用户能定

| # | 事项 | 说明 |
|---|---|---|
| B1 | **分支怎么收** | `feat/pen-clamp-redesign` 未合并、未推送、未开 PR（用户 2026-09-21："先不提交"）。⚠ **这条分支还含更早几个月的工作**（笔夹重设计、力反馈模块等），合并会一并进 `master` |
| B2 | **要不要恢复被覆盖的旧报告** | 上一计划的 `task-1-report.md`（约 54 KB）被**跨计划通用名派单**覆盖；`.superpowers/sdd/` 全量 gitignore ⇒ git 恢复不了。旧内容**仍在会话转录库**里，但复制出来被权限分类器拦下 —— **需要用户同时指名来源与目的地** |
| B3 | ~~两份文档要不要提交~~ **已解决** | 用户 2026-09-21 定：**提交**。上机清单与本文档已入库（`a4843cc` 起，后续修订见 `650fc4a` / `375f719`） |

---

## C. 不要做的事（写下来免得将来有人"顺手修好"）

| # | 别做 | 为什么 |
|---|---|---|
| C1 | **别为了让套件好看而改那条刻意留红的断言** | 它问的是四份夹具答不了的问题。让它变绿 = 把"未验证"伪装成"已验证"（用户明确否掉过这个交易） |
| C2 | **别在拿到接触状态数据前改 `Fz` 的掩码** | 它不投票的依据已被推翻，但新依据（`@720` 在接触下是什么样）**还没有**；符号约定也未验证 |
| C3 | **别把"有单测覆盖"说成"已在实机验证"** | 至今**没有一条**实机验证。漂移检查只做到"可测 + 逐分支覆盖" |
| C4 | **别说套件"全绿"** | 现状（截至 `7beece8`，2026-09-21 晚）`test_force_compensation` **39/0**、`test_payload_calibration` **74/1**，那 1 条是刻意留红。⚠ 本行先后写过 `69/1`、`71/1` —— **都过期过**（`2168be0` / `54475de` / `7ef1646` / `7beece8` 各加了用例）。**计数会随用例增减漂，引用前重跑** |
| C5 | **别丢那笔安全债的账** | 容差从 `0.50` 放宽到 `1.2464 N`（灵敏度降 **2.49×**）：质量失配 ≥ 0.127 kg（≈ 载荷 31%）仍被拒（含真踩过的 `0.66` vs `0.4159 kg` ⇒ 2.39 N，余量 1.92×），**低于此的中等误差会静默通过** |
| C6 | **别把 `@720` 的符号约定当成"已解决"** | 它只在静止态被验证过（静止时两边都 ≈0 ⇒ 零信息量）。见上机清单 §5 |

---

## D. 今晚的结果 → 下一步（拿着数据回来照这张表走）

| 今晚的项 | 结果 | 下一步 |
|---|---|---|
| **§5 符号约定** | **同号** | 符号约定这条**从"未验证"降级为"已确认"**，可以把它从收口项里划掉 |
| | **反号** | **停下，按【设计问题】处理** —— 真实外力下闸门会看到约 2× 偏差。**不要当成调参** |
| | 未做 | 仍是收口项；下次上机优先做它（**它是唯一无法用离线数据替代的**） |
| **§1 新采集** | **放行** | **独立验证达成** ⇒ 可以拿它支撑 `Fz` 投票与容差放宽的后续决定（这是第一份独立证据） |
| | **拒绝** | **真发现**：带原始输出回来，先查**容差与本地模型的哪一项**不符，别先怀疑采集 |
| | 套件**失败**且消息说"参考量不可用" | 回去核**陷阱 1**（记录程序没写满末尾六列），重采 |
| | 打印 `未找到 calib_poses_2026-09-21.txt` | 名字/位置不对（**不是失败**）—— 必须一字不差地叫这个名字 |
| **§2 `@1037`** | `== 1` | 判据依据从**历史记录**升级为**实测** ⇒ 这条收口项可划掉 |
| | `!= 1` | **改的是判据**（不是排故障）；届时与"参考量可用性"那条一起重新设计 |
| **§3 闸门是否放行** | 放行 | "传感器力那一路"**首次在实机上真的通** |
| | 拒绝 | 看逐通道表是哪一路超限；若是力矩 ⇒ 不该发生（它们不投票），查掩码 |
| **§4 漂移检查** | 报"正常" | 无动作 |
| | 报漂移（> `0.5 N`） | 按它给的两条建议判断：零部件动过 ⇒ `'m'`+`'s'` 重标；否则 ⇒ `'z'` 重调零 |
| **§7 砝码** | 有读数 | `A` 第三行**首次有了绝对界的素材**（此前完全没有）⇒ 可据此设计"第三行守门" |
| | 未做 | `A` 第三行仍**无绝对界** ⇒ 那条收口项保留 |

**拿到数据之后要做的事（离线）：**
1. 把新夹具**入库**（它是独立证据，必须落盘 —— 本项目规矩："没落盘的等于不存在"）；
2. 把 §1 的结果写进**计划的收口表**，并把 B1 那条从"必做"改成按结果分流；
3. 若 §5 有结论 ⇒ 更新 `Fz` 决策的依据（任务 6 只推翻了旧依据，没给出新依据）；
4. 复核本文件 A 节里那些"注释与代码不符"的项 —— 它们不影响结论，但会成为下一个人误信的来源。
